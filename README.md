# Suture

面向不熟悉技术细节的同事的 AI Gate 连通工具：双击打开，一次「开始检查」看这几个
客户端能不能连上公司网关（AI Gate），能连上就是绿勾；连不上才展开「可能的原因」，
每一条都用大白话讲清楚是哪儿的问题、怎么修，能安全自动修的直接给按钮，需要你提供
Key 或选模型的给输入框 / 候选列表，处理完再测。另有一个「安装 / 上手」页，帮没装过
的同事用官方命令装好缺失的客户端并配置连 AI Gate。

覆盖三个客户端：Claude Code CLI、Codex CLI、DeepSeek Harness。

## 跑起来

```bash
python3 main.py                  # 图形界面
python3 main.py --cli            # 命令行
python3 main.py --cli --no-fix   # 不修改任何配置文件
```

`--no-fix` 只保证 **Suture 自己**一个字节都不写；它仍会让客户端跑一次自证
（`claude -p` / `codex exec`），那一步由客户端自己写状态目录、也会消耗一次调用。

图形界面会优先用系统自带的 WebView 开一个应用窗口；初始化不成功（主要是 Linux
上没装 WebKitGTK）就退回默认浏览器，界面内容一样。设置 `SUTURE_NO_BROWSER=1`
可以只起服务、不主动拉起浏览器。

## 打包

```bash
./build/build.sh                 # 在当前这台机器上打一个单文件可执行程序
```

六个平台组合需要六台对应系统/架构的机器——PyInstaller 不能跨系统交叉编译，
这是工具本身的限制。`.github/workflows/release.yml` 里是六条并行的构建流水线，
macOS 用 universal2 把两种架构合成一个文件。

## 验收测试

```bash
python3 -m unittest discover -s tests -t .
```

测试不依赖真实网关：`mock_gateway.py` 起一个假网关，可以让它表现成正常、
5xx、限流、鉴权拒绝、跑着跑着开始出错等各种情况，用来触发各条判断分支。
每个用例都用独立的临时 HOME 和项目目录，不会碰到你自己的配置。

## 代码结构

| 位置 | 职责 |
|---|---|
| `suture/profile.py` | 加载网关规则（地址、鉴权、模型路由表），预留了从网关运行时拉取的接口 |
| `suture/harness/` | 三个客户端各一个适配器，把各自的配置结构归一化成同一套字段；另负责各自的命令探测、官方安装命令、Key 落点 |
| `suture/checks.py` | 检测项。连通失败时才作为诊断器跑，把静态问题翻译成「可能的原因」清单 |
| `suture/gateway.py` | 出网请求与响应分类 |
| `suture/fixer.py` | 备份、写入 |
| `suture/selftest.py` | 让客户端用**它自己**的配置自证能否使用（只在本机没法发验证请求时才做） |
| `suture/installer.py` | 探测 Node 运行时、执行各客户端官方安装命令并回收输出 |
| `suture/engine.py` | 单客户端四态判定（连通 / 连不上 / 没配置 / 没安装）、逐条修复动作、一键配置、整体编排；命令行和界面共用 |
| `suture/server.py` | 界面用的本地接口（state/check/action/configure/recheck/install_run），只监听 127.0.0.1 并带一次性令牌 |
| `suture/ui/index.html` | 界面本身，自包含，不引任何外部资源；「检查」「安装 / 上手」两个页 |
| `gateway_profile.json` | 网关规则。网关新增模型或调整协议时改这里，不用重新分发二进制 |

设计说明见 `DESIGN.md`，验收记录见 `VERIFICATION.md`，产品侧的判断依据见产品方案文档。

## 语义（每个客户端四态）

- **已连接（绿勾）**：真实请求能连上 AI Gate。通了就只给一个绿勾，不再罗列
  任何细项——跟连通无关的边角问题不管。
- **无法连接**：才展开「可能的原因」。原因分四类：
  `自动修复`（地址少协议、多拼 /v1、模型名大小写、**Key 没放进网关要求的
  Token 请求头**等，给「修复」按钮）/ `选模型`（模型不在网关列表，给候选下拉，
  只改目标、保留清单里其它模型）/ `填 Key`（Key 缺失或补头后仍被网关拒绝，
  输入网关后台生成的 Key）/ `外部处理`（例如 Key 得回后台重新生成、Codex 项目
  要标记可信、**Codex 这种带不了自定义请求头因而连不上 AI Gate 的客户端**，
  只给说明）。处理完用主按钮再跑一次「开始检查」验证。
- **未配置**：装了客户端但没连网关，给「配置」面板（默认模型 + Key）。
- **未安装**：引导去「安装 / 上手」页装。

## 几条守住的边界

- **Key 不出本地。** 只用于向网关发请求和写回本机配置；界面、日志、接口返回里
  一律只出现掩码后的形式（前 4 位 + 后 4 位）。四条会返回客户端数据的接口
  （检查 / 修复 / 配置 / 重测）共用同一个脱敏出口，抹掉修复载荷里的原始 Key；
  网关抛出的异常原文也不外传（它可能带着请求头的值）。
- **读不懂的文件不写。** 配置有语法错、或不是 UTF-8（记事本存成 ANSI/GBK 就会这样）
  时，Suture 如实报错并**拒绝写入**——按读空的文件写回会把同一份配置里其它内容
  （permissions、hooks、其它插件配置）静默清掉。这条覆盖所有会被整份重写的文件，
  包括 DeepSeek 存各家 Key 的 `.credentials.yaml`（读不懂照写会把别人的 Key
  一起覆盖掉）。这类问题只给说明，让用户先修好格式。判定时按「这次要写哪个文件」
  算：一份坏的凭据文件只影响鉴权那些项，不会连地址、模型的修复按钮一起封掉。
- **连通优先，成功不罗列。** 判断标准只有一个：真实请求能不能通。通就过，不把
  几十项细粒度静态检查摊在成功用户面前；静态检查降级成「连不上时才跑」的诊断器。
- **决定性请求用客户端自己配的模型发。** 模型名配错了就要被如实标出来，不能拿
  探活模型（网关保证认识）去试、把坏模型名掩盖成已连通。
- **网关侧有问题时不动本地配置。** 探的是规则里那个已知正确的地址，先确认网关
  本身活着；超时、连不上、限流、5xx 直接停下。鉴权被拒不算网关侧问题——那更可能
  是本地 Key 配错了，正是要抓的场景。
- **写配置永远先备份，而且写入是原子的。** 任何一次写入（修复、存 Key、一键配置）
  都先把原文件备份下来；真正落盘时也是「内容全部准备好 → 写临时文件 → 原子替换」，
  不是先截断再写。所以即使序列化失败、编码失败、写到一半被打断，用户的原文件也
  逐字节不动，而不是被清空或留个半截。一键配置失败时会把刚生成的配置一起回滚掉，
  不留下「有地址没 Key」的半成品。
- **不替用户做决定。** 一键配置要用户真的选了模型才写，型号不在网关清单里就什么都
  不写——不会静默换成别的模型让用户「要 A 得到 B」。
- **同一时刻只跑一个安装。** 安装可能跑几分钟（所以不占着主锁，界面其它操作照常），
  但也不会两个并发跑——两个 `npm install -g` 写同一个全局前缀，结果不可预期。
- **只改实际生效的那一层。** Codex 的项目级配置在项目不可信时整层不生效，这种
  情况下不会去写它——写了等于没改。
- **不猜。** 配置文件读不懂就如实报错，不按猜测继续。模型名只在「明显是打字错误」
  （大小写、连字符写法）时自动纠正；如果纠正后的候选本身也是路由表里另一个独立
  型号（比如 `gpt-5.6` 与 `gpt-5.6-sol`），一律不自动改，交给用户确认——那会让人在
  不知情的情况下连到另一个模型，比直接报错更糟。

## 已知限制

- **DeepSeek Harness 的 YAML 用的是自己写的受限读取器。** 标准库没有 YAML 解析器，
  而引擎要保持只依赖标准库。支持嵌套映射、列表、标量、引号、注释；遇到锚点、块标量、
  流式写法、多文档会明确报错而不是猜。写回用户层 `settings.yaml` 时不保留原有注释
  （写前有备份）。
- **协议版本头检测默认关闭。** 网关是否校验 `anthropic-version` 尚未确认，确认后把
  `gateway_profile.json` 里 `protocol_version.check_enabled` 改成 `true` 即可，不用改代码。
- **Codex 的 base_url 需要 `/v1` 后缀（已实测）。** 网关 OpenAI 形态路由
  `/v1/chat/completions` 真实存在、带 Token 头可 200，所以 root + `/v1` +
  `/chat/completions` 的拼法是对的；Codex 连不上 AI Gate 只差在它带不了 Token 头。
- **AI Gate 只从 `Token` 请求头读 Key，而 Codex 无法携带自定义请求头——Codex 现状连不上 AI Gate（均已实测）。** anthropic（`/v1/messages`）与 openai（`/v1/chat/completions`）两种形态都只认 Token 头；OpenAI 形态路由存在且可用，Codex 的 base_url 拼法（root + `/v1` + `/chat/completions`）是对的。但 Codex CLI 没有可附加自定义请求头的位置，只会把 Key 放进 `Authorization: Bearer` → 一律 401「个人apikey认证失败」。所以 Suture 对 Codex 给「外部处理」说明（改用 Claude Code / DeepSeek，或等网关放开其它头），不引导它填 Key / 补头。Claude Code 靠 `ANTHROPIC_CUSTOM_HEADERS` 带 Token；DeepSeek 写在路由的 `headers.Token`（Suture 存 Key / 补头都写这里）。网关采信哪个头由 `gateway_profile.json` 的 `auth.required_header` 声明，改了它判定自动跟着变。
- **型号清单没有可拉的端点，目前靠内置快照。** GET `/v1/models`、`/models`、`/v1/model/list` 等对真实网关都返回 404，`profile.py` 的运行时拉取接口也尚未接通（预留）。模型表更新仍走「改 `gateway_profile.json` + 重新分发」，网关哪天提供型号端点，接上 `fetch_remote_profile()` 就能活拉。
- **Claude Code 的地址/模型若来自系统环境变量，Suture 只给外部指引。** 系统环境变量
  `ANTHROPIC_BASE_URL` / `ANTHROPIC_MODEL` 优先级压过配置文件，而 Suture 只写配置文件、
  改不到你的环境变量——这种配置连不上时不给「一键修复」（写了不生效、旧逻辑还会把
  环境变量里的错误值复制回文件造成原地循环），而是明确告诉你去「系统设置 → 环境变量」
  里改或删对应变量再重试。同一套判断也覆盖「生效值来自公司统一下发的托管配置」：
  那种情况本地改了也不生效，提示去找 IT，不给按钮。
- **权限收紧只在 macOS / Linux 生效。** 备份目录与含 Key 的配置文件会用 `chmod 600`
  收紧权限，但 Windows 上 `os.chmod` 只映射只读位、不做访问控制——Windows 下依赖
  用户目录本身的默认 ACL。
- **DeepSeek Harness 的二进制探测看配置目录，不看 PATH。** 实测本机 `~/.dsh` 存在但 PATH 里没有 `dsh` 命令，所以 `binary_name` 留空、`detect()` 按目录判「已安装」——这比按命令探测可靠（按命令会误报「未安装」）。代价是「没装却留了空配置目录」这种边角会被当成已装，可接受。检查页与安装页的判装已用同一套依据（`detect_binary` 也看目录），不会再出现一个页面说已装、另一个说未装。
- **DeepSeek Harness 还没确认可自动执行的官方安装命令 / 非交互自证方式。** 装与测
  相关能力对它是给引导而不是自动执行；确认了正确命令后补齐即可，不用改其它逻辑。
- **签名尚未接入。** macOS 不签名 + 不公证的话 Gatekeeper 会直接拦截，非技术用户打不开；
  Windows 会弹 SmartScreen 警告。证书就绪后在构建流水线里补上这两步。

