# Suture · 能力汇报

**一句话**：给不熟悉技术的同事用的一个本地小程序，双击打开、点一次「开始检查」，
就能知道本机的 AI 客户端（Claude Code CLI / Codex CLI / DeepSeek Harness）
能不能连上公司网关 AI Gate；连不上时直接告诉他是哪儿的问题、怎么修，能安全自动
修的就给按钮，处理完再测一次。

**交付形态**：单个可执行文件（Windows / macOS / Linux，各两种 CPU 架构），
不需要装 Python、不需要装任何依赖，不联网下载东西（除了向网关发验证请求）。

---

## 一、它怎么判：只看「能不能用」

判定标准只有一条：**用客户端自己配的那套配置，真实发一个请求出去，通不通**。

| 结果 | 界面表现 |
|---|---|
| **已连接** | 一个绿勾，不罗列任何细节 |
| **无法连接** | 展开「可能的原因」清单，每条=一句人话+怎么修，用户挑着处理 |
| **未配置** | 装了客户端但没连网关 → 给配置面板（选模型 + 填 Key，一步到位） |
| **未安装** | 引导去「安装 / 上手」页，用官方命令直接装 |

设计上刻意**不**做逐项静态体检：非技术同事要的是「能不能用」，不是「哪里跟规范
不一致」。静态检查降级成「连不上时才跑的诊断器」——健康用户不会看到满屏报错。

---

## 二、能发现并**自动解决**的问题（按按钮即可）

这些点一下就能修好，改完立刻重测，通不通当场反馈。

| # | 能发现的问题 | 用户看到什么 | 做了什么 |
|---|---|---|---|
| 1 | 地址前后多了空格 | 「网关地址不对」 | 去掉空格 |
| 2 | 协议写成了 http（网关要求 https） | 同上 | 补成 https |
| 3 | 地址少了 `https://` 前缀 | 同上 | 补前缀 |
| 4 | 地址结尾多拼/少拼一段 `/v1` | 同上 | 按该客户端的拼路径习惯改对 |
| 5 | 地址跟网关登记的不一致 | 同上 | 改成登记地址 |
| 6 | 根本没配网关地址 | 同上 | 写入网关规范地址 |
| 7 | **Key 没放进网关要求的请求头**（AI Gate 只认 `Token` 头，很多人放进了 `x-api-key`） | 「Key 没放进网关要求的 Token 请求头」 | 自动把 Key 写进 `headers.Token` / `ANTHROPIC_CUSTOM_HEADERS` |
| 8 | Key 前后带了空格或换行（复制粘贴常见） | 「Key 前后有多余的空格或换行」 | 去掉首尾空白 |
| 9 | 模型名只差大小写/连字符写法（如 `GLM-5.3`） | 「模型名字只差大小写或写法」 | 改成网关登记的写法 |

> 第 7 条是本次实测最有价值的一条：**同样一把有效 Key，放进 `x-api-key` 一律 401，
> 只有放进 `Token` 头才 200**。以前工具会误导用户「Key 失效了，去后台重新生成」，
> 现在能直接点按钮补上。

---

## 三、能发现、需要用户**做一次选择或粘贴**的问题

| # | 能发现的问题 | 用户要做的 |
|---|---|---|
| 10 | 完全没配鉴权信息 | 粘贴一个网关后台生成的 Key |
| 11 | Key 确实失效/被后台吊销了（补头之后仍然 401） | 重新生成 Key 后粘贴 |
| 12 | Key 不是网关签发的（会自动识别 Anthropic / OpenAI / Google 等厂商前缀，指出「这是别家的 Key」） | 换一个正确的 Key |
| 13 | 模型不在网关路由表里，但有名字相近的**独立**型号 | 从候选下拉里选（**不自动替换**——会自动让人不知情地连到另一个模型，比报错更糟） |
| 14 | 模型完全不在路由表 | 从网关完整型号清单里选一个 |
| 15 | 没指定模型，且真实请求没跑通 | 从清单里选一个默认模型写进去 |
| 16 | DeepSeek 一次注册了多个模型、坏了其中一个 | 只换掉坏的那个，**其余模型原样保留** |

---

## 四、能发现，但只能在工具外处理（给明确说明，不误导）

| # | 能发现的问题 | 告诉用户什么 |
|---|---|---|
| 17 | 配置文件 JSON/TOML/YAML 语法错，整层配置不生效 | 指出文件位置和错误行列，让改完重新检查（**并拒绝自动修改**，见第五节） |
| 18 | **文件不是 UTF-8 编码**（记事本存成 ANSI/GBK） | 说明整份文件读不出来，让另存为 UTF-8 |
| 19 | 字段名拼错（写了工具不认识的键，等同没配） | 给出最接近的正确字段名 |
| 20 | 配置存在但整层不生效（Codex 项目没被标记为可信） | 说明「改这一层没用」，指出该改哪一层 |
| 21 | 生效值来自**系统环境变量**（`ANTHROPIC_BASE_URL` 等，优先级压过配置文件） | 说明工具写不到环境变量，让去「系统设置 → 环境变量」改或删 |
| 22 | 生效值来自**公司统一下发的托管配置**（IT 下发、只读） | 说明本地改了不生效，请联系 IT |
| 23 | 同一项在**多层配置**里取值不一致 | 说明改哪一层、让用户把其它层统一掉 |
| 24 | 同一个 Key 在多个位置重复设置、值还不一样 | 摆出证据（哪个更像网关签发的 Key），让用户确认后删掉多余的 |
| 25 | **客户端带不了网关要求的请求头**（Codex 只能发 `Authorization`，无法附加自定义头） | 直接说明这条路走不通，建议改用 Claude Code 或 DeepSeek；**不引导用户白填 Key** |
| 26 | 型号在清单里、地址也对，但网关实际拒绝（型号下线/改名） | 说明可能已下线，联系网关确认 |
| 27 | **请求在本机就没发出去**（Key 带换行导致请求头非法等） | 点明是本地问题，让先处理下面的本地项——**不再误报成「网关侧故障，联系值班人员」** |

---

## 五、写配置的四条安全约定

1. **永远先备份**。任何一次写入（修复 / 存 Key / 一键配置）都先备份到
   `~/.suture/backups/<时间戳>/`，出问题能人工找回。
2. **Key 不出现在界面上**。界面、日志、命令行、接口返回里一律只有掩码
   （前 4 位 + 后 4 位）；网关抛出的异常原文也不外传（它可能带着请求头的值）。
3. **读不懂的文件不写**。配置有语法错或编码不对时，如实报错并**拒绝写入**——
   按读空的文件写回会把同一份配置里其它内容（permissions、hooks、其它插件配置）
   静默清掉。宁可写不了，也不能悄悄清空别人机器上的配置。
4. **写不进去的不给按钮**。生效层是环境变量或托管配置时，自动修复写了也不生效，
   这种不给「修复」按钮（避免用户点了没反应、原地打转），只给说明。

---

## 六、它不负责什么（边界，说明白）

- **不判断「是不是直连网关」**：只要真实请求能通就判已连接。客户端把地址指向一个
  本地转发服务、请求返回 200 时也算通——口径是「能不能用」，不是「有没有按规范配」。
- **不比对公司规范**：不做配置项的合规审计，不检查「有没有多余字段」这类问题。
- **不改系统环境变量**：环境变量层面的配置只给指引。
- **不代管 Key**：Key 只在用户自己的机器上流转，不回传、不上报。
- **网关规则靠内置快照**：型号清单更新目前需要更新规则文件重新分发（网关尚未提供
  可拉取的型号接口）。

---

## 七、当前状态

| 项 | 状态 |
|---|---|
| 功能 | 三个客户端 × 四态判定、逐条修复、一键配置、安装引导、图形界面，全部可用 |
| 自动化测试 | **121 条，全部通过**（1 条平台限制跳过）（不依赖真实网关，用假网关覆盖各条判断分支） |
| 实测 | 已在真实网关（tower-ai）上做过端到端验证：Claude Code 与 DeepSeek 直连可用；Codex 已定性为契约层面连不上（网关只认 `Token` 头，Codex 无法携带） |
| 打包 | 六个平台组合的构建流水线已就绪；Linux x64 上实际打包运行过，其余五个需在对应机器上各跑一次 |
| 待办 | 代码签名（macOS 不签名用户打不开）、DeepSeek 官方安装命令与非交互自证方式、Windows 下的文件权限收紧 |

---

## 八、给同事的一句话用法

> 双击打开 → 点「开始检查」→ 有绿勾就没事；有红/黄就照着上面的说明点一下按钮，
> 或者把说明截图发给 IT。装客户端去「安装 / 上手」页。
