# 验收记录

`python3 -m unittest discover -s tests -t .` —— 86 条，全部通过（1 条在 root 下跳过，
已另行以普通用户身份验证）。测试不依赖真实网关，用 `mock_gateway.py` 起一个假网关，
让它表现成各种情况来触发每条判断分支。

> **2026-09-09 起语义改为「连通体检 + 引导修复 + 安装上手」**，见下方
> 「2026-09-09 重构为连通优先」一节。旧文档里讲到的「阶段一/二/三」「一键修复(N 项)」
> 「修完验证失败自动回滚」「重新检查按钮」等都属于重构前的语义，已不再适用。

---

## 2026-09-09 重构为连通优先

**目标语义**：只判每个客户端能不能连上 AI Gate，通就绿勾、不罗列任何细项；连不上才
展开「可能的原因」，每条原因=人话+怎么修，按修复方式分类（自动修复按钮 / 模型候选下拉 /
粘贴 Key / 外部处理）；装了没配的给配置面板；没装的引导去「安装 / 上手」页。另有安装页
自动执行官方安装命令。

**四态判定顺序**（`suture/engine.py::assess_client`）：
无配置 + 无二进制 → `not_installed`；无配置但有二进制 → 跑客户端自证（通=`connected`，
不通=`unconfigured`）；有配置 → 用**客户端自己配的模型**发真实 e2e 请求，任一成功=
`connected`（成功路径不跑静态体检），全失败=`blocked` 并调用 `checks.blocked_reasons`
跑诊断、按严重度出原因清单。顶层 `probe` 判为网关侧（超时/连不上/限流/5xx）时短路：
每个客户端只给一条 `external` 的「网关侧连不上」，完全不碰本地配置。

### 这轮覆盖的新语义（对应新增/改写的测试）

| 场景 | 验证 |
|---|---|
| 连通优先 | 配置能连通时，即使静态项有问题（如 settings.json 多了个不认识的顶层字段）也判 `connected`、issues 为空 |
| 成功不罗列 | 正确配置 → `connected`，无任何明细 |
| e2e 用配置模型 | 配了坏模型名 → `blocked`，最后失败那次请求用的是配置模型而非探活模型；模型不在路由表 → `choice` 原因 |
| 网关侧短路 | 5xx/限流 → 顶层 `gateway_down`、配置逐字节没动；每客户端只有一条 `external` 原因，apply 它返回 `manual`、不改文件 |
| 单条动作 | auto（地址多拼 /v1、模型大小写 typo）/ choice（候选下拉）/ input（粘贴 Key）各能修成 `connected` |
| 模型只改目标 | DeepSeek 注册多个模型时，把坏的换掉、其余模型原样保留（`zzz-model-a→glm-5.3` 后 `zzz-model-b` 仍在） |
| 环境变量 Key（codex） | 项目不可信时修复写用户级 config.toml；Key 走 env_key 引用 + `setx`（仅真实运行且 Windows 时），配置里永不明文 |
| DeepSeek Key | 存 `.credentials.yaml` 的 refs，权限 600 |
| 一键配置全新用户 | 无配置沙箱下 `configure_client` 生成最小配置 + 存 Key → `connected` |
| 安装执行器 | 官方命令/覆盖命令真实跑并回收输出；缺 Node 给 nodejs.org 引导；无命令只给引导；超时强杀进程树不挂死；未知客户端 400 |
| 服务端接口 | state 含 3 客户端 + 运行时探测；check→action 修复流；action 先于 check=409；Key 不外泄；recheck/configure/install_run |
| Codex 契约不可达 | codex 配好地址和 Key、在只认 Token 头的网关上 e2e 仍 401 → 只给单条 external「auth-incompatible」（不再给填 Key/补头）；configure_client 对 codex 直接拒绝、不写配置/env；判定契约驱动（required_header × 适配器能力），required_header 改回 Authorization 时同一个 codex 不算不可达；claude/deepseek 在 token_only 下仍走「补 Token 头」自动修复 |
| 安全回归 | Key 只以掩码出现；仅 127.0.0.1 + 一次性令牌 |

### 过程中发现并修掉的问题（这轮新增）

1. **Windows 上客户端自证会无限期挂死。** `selftest.run` 原先用 stdout/stderr 管道承接
   真实客户端的输出；像 Codex 这种 launcher 会再派生孙进程，孙进程继承了管道写端——
   父进程退出后 `communicate()` 仍等不到 EOF。实测一台装了 Codex 但没连 AI Gate 的机器，
   体检在自证一步卡 90 秒以上不返回（`subprocess.run` 超时杀掉父进程后 Windows 还会再等
   一次 `communicate()`，仍然不返回）。改成把输出写进临时文件、等「父进程退出」而非
   「管道关闭」，超时用 `taskkill /T` 强杀整棵进程树。安装执行器用同一套承接方式。
2. **CLI 子进程测试不封闭。** `test_cli.run_cli` 组装子进程环境时没带上
   `SUTURE_SELFTEST_COMMAND`，导致装了但没配的客户端（本机真实 codex）去跑真实的
   客户端自证——测试结果取决于跑测试这台机器装没装、登录没登录，同一份配置有时绿、
   有时红、有时直接超时。改成默认注入隔离命令；断言整体结果的 CLI 用例限定
   `--harness claude_code`，只把「网关侧短路」那条保留三客户端全跑（网关 down 时短路、
   不触发自证，仍然确定）。

### 尚未验证（2026-09-09 新增）

- DeepSeek Harness 的官方安装命令与非交互自证方式：`install_command()` /
  `self_test_command()` 都返回 None，安装页与自证对它只给引导，不自动执行。
- Claude Code / Codex 的 npm 安装命令需在干净 Windows 机器上端到端跑一次（含装完
  PATH 刷新、`claude` 二进制探测边界——npm 全局 bin 进 PATH 后新终端才生效）。
- Codex 的 `setx AI_GATE_API_KEY` 不可备份/不可回滚，真实机器上只验证了引导文案与
  code 路径（测试环境 env≠os.environ 时不会真的执行 setx）。
- Codex 的 npm 包名与干净机安装端到端仍需确认；base_url 带 `/v1` 后缀已实测确认
  （真实网关 OpenAI 形态 `/v1/chat/completions` 路由存在、带 Token 头返回 200）。
- Codex 能否连 AI Gate：已实测定性为「不能」——Codex 没有自定义请求头机制、只发
  `Authorization`，而网关只认 `Token`（两种形态都只认 Token）。Suture 据此给外部
  处理，不再引导填 Key（详见下方第 17 条）。

---

## 覆盖了什么（2026-09-09 之前语义）

| 范围 | 验证内容 |
|---|---|
| 主流程 | 配置正确一路通过；发现问题→修复→重验证成功；修复后配置文件内容确实变了；再跑一遍是干净的 |
| 网关侧判定 | 5xx / 限流 / 连不上 → 判定网关侧问题并且**逐字节确认配置文件没被动过**；401 不算网关侧问题，继续查本地 |
| 归因逻辑 | 修复后网关恰好挂了 → 保留修复不回滚；网关正常但仍连不通 → 回滚，并确认文件恢复原样 |
| 三个客户端 | 各自的配置结构、优先级、鉴权形态分别验证；同时装了多个时分别出结论 |
| Codex 可信项目 | 项目不可信时那一层不生效、被标出来，修复只写用户级——不去写一份改了也没用的文件 |
| DeepSeek Harness | 用户层覆盖基线；Key 四层优先级；headers.Token 写法；注册的每个型号都校验；基线文件不被改动；修复其中一个模型不会把其它已注册的模型顶掉 |
| 模型名纠正 | 大小写/连字符差异自动纠正；`gpt-5.6` 与 `gpt-5.6-sol` 这类并列变体一律不自动改，改成给候选菜单 |
| 模型未配置 / 名字不确定 | 不会替用户填任何一个模型名（包括探活用的模型），而是把网关支持的完整型号清单或相近候选做成可点的菜单，用户选了才写入，走完整的备份/验证/回滚流程 |
| Claude Code 自定义请求头 | `ANTHROPIC_CUSTOM_HEADERS` 能被正确识别为跟 `API_KEY`/`AUTH_TOKEN` 平行的鉴权来源；只有它生效时不再误报「没有配置鉴权信息」；跟主字段同时生效且内容不一致时会明确指出冲突，并在能判断时指出哪个更像网关签发的 Key |
| 端到端结果与静态判断的取舍 | 端到端真实请求已经证明「当前地址+鉴权确实连得通」（比如走了本地转发）时，跟连通性直接相关的静态判断不再继续报错，也不会再被「一键修复」误改 |
| 全新用户 | 三个客户端都走「生成配置」而不是「挑错」；生成的地址按各自惯例（Codex 带 /v1）；模型字段留明显的占位提示而不是塞一个猜的值；凭据文件权限 600 |
| 安全 | Key 在报告、接口返回、命令行输出里都只有掩码形式；备份文件权限 600；接口只监听 127.0.0.1 且必须带一次性令牌 |
| 异常路径 | 配置文件写不进去时如实报告失败、不谎称成功；YAML 读不懂时报错不猜；规则文件坏了明确失败；回滚会删掉修复时新建的文件 |
| 打包产物 | PyInstaller 打出的单文件二进制实测可运行，界面和规则文件都在包里，令牌保护生效 |
| 组织托管配置（managed-settings.json） | 优先级压过全局/项目级配置，但真实存在的环境变量仍然更高；只靠这一层存在也能被探测到；不算进可写路径，不会被备份/回滚流程碰到；本地设置被它覆盖时判定成「改了也没用」，不提供一键修复 |
| 网关多地址（`alternate_roots`） | 首选地址连不上、登记的另一个地址是活的时，阶段一不会误判成网关侧问题；阶段二检查用户填的地址时，填的是登记的任意一个都算对；登记的地址全部连不上时仍然正确判定成网关侧问题 |
| 客户端自证 | 客户端里什么都没配、但客户端自己实测能跑通时，「没配置地址/鉴权」不再报成故障，也不给一键修复按钮；实测跑不通时照旧报"没配置"；连配置文件都没有但实测能用时，不再提示"帮你生成一份配置"（那会盖掉正在工作的连接）；已经配好的机器不会多花一次真实调用去自证 |
| 模型名未指定 | 不填模型名默认判成正常（型号清单收进折叠区，不平铺成报错），但端到端确实没跑通、又没指定模型时会升级成需要处理的问题并摆出清单 |
| 探活专用 Key（`probe_key`） | 配置了之后，阶段一探活不依赖用户自己有没有配置、配得对不对，全新用户也能正确显示网关本身是活的；留空时退回到原来「借用户配置里的 Key」的行为 |

## 过程中发现并修掉的问题

1. **验证请求的路径写死成了 `/v1/messages`。** Codex 走 OpenAI 兼容协议，base_url 自带
   `/v1`、只在后面拼 `/chat/completions`，之前会拼成 `/v1/v1/messages` 打到 404——
   结果是「明明修好了却验证不通」，被误判成修复无效直接回滚。改成按 harness 各自的
   真实请求形态发。
2. **阶段一探的是用户配置里的地址。** 用户把地址填错成一个连不上的域名时，工具会连不上，
   然后判定「网关侧问题，不要动你的配置」——而真正该修的恰恰就是这个地址。改成探规则里
   那个已知正确的地址，先确认网关本身活着，再判断本地配置。
3. **读取时把值 strip 掉了。** 地址前后的多余空格在读的时候就被抹平，检测不出来，
   但真实客户端发出去的是带空格的值。改成原样保留，由检测项发现并修复。
4. 假网关的行为不够真实（网关自身故障应该先于路由发生），一并修正，否则测试验证的
   不是真实场景。
5. **模型没配置时会把探活用的模型悄悄当成「修复建议」写回真实配置。** 探活用的模型只是
   用来验证网关活不活着，跟用户实际想用哪个模型是两回事，这么做等于替用户决定了从没问过
   的答案。生成全新配置时也有同样的问题。改成两处都不再自动填，交给用户从菜单里选。
6. **Claude Code CLI 完全没有读取 `ANTHROPIC_CUSTOM_HEADERS`。** 真实场景里网关鉴权可能
   走这个自定义头（比如 `Token`），而不是 `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN`——
   之前 Suture 对这个变量视而不见，会把探活用的鉴权值取错，误判成「鉴权失败，是本地配置
   问题」。改成把这个变量解析出来，跟主字段并列参与判断，两边同时生效且内容不一致时
   明确提示冲突。
7. **端到端真实请求已经验证通的配置，还会被静态判断打成「有问题、可自动修」。** 例如
   客户端配置指向一个本地转发地址，跟规则文件里登记的地址不一致，但实际请求是通的——
   静态判断这时候如果还坚持「地址不对」，一键修复可能会把一套本来正常工作的配置覆盖掉。
   改成端到端已经验证连通时，跟连通性直接相关的静态判断让位于真实结果。
8. **DeepSeek Harness 修复某一个注册模型时，会把整份模型清单换成只有这一个。** 无论是
   自动纠正打字错误还是新做的候选菜单，只要涉及"改其中一项"，都要保留清单里其它已经
   注册好的模型，不能因为改一个就把别的顶掉。
9. **Claude Code CLI 完全不认组织托管配置（managed-settings.json）。** 公司通过 IT/MDM
   统一下发的配置优先级最高，用户自己看不到也改不了，这也是有些用户「从来不用自己填
   网关地址」的真实原因——之前 Suture 只读全局和项目级两层，读不到这一层的值，会把
   实际生效的地址/模型漏判、或者错误地建议改一个改了也不会生效的文件。改成新增这一层，
   只读不写，也不算进备份/回滚和可写路径的范围；本地设置被它覆盖时不再提供一键修复，
   而是明确告知「这是公司统一下发的，本地改了也没用」。
10. **阶段一网关探活只测规则里那一个 canonical 地址，网关如果不止一个能进的地址就会
    误判。** 真实反馈的场景是有些用户走的是网关登记的另一个入口（不是常规转发地址），
    这个地址在规则文件里压根没登记过，阶段一探不通就直接判定「网关侧问题」并整体跳过
    阶段二——用户看到的现象是「网关连不上」和「查不到模型」同时出现，其实是同一个
    根因的两个表现，不是两个独立的 bug。改成规则文件支持登记多个有效地址
    （`alternate_roots`），阶段一挨个试，阶段二检查用户填的地址时命中任意一个都算对。
    同时把阶段一探活用的鉴权来源换成一个独立的 `probe_key`（专门生成的极小权限
    Key），跟用户自己的配置解耦——不然全新用户什么都没配的时候，探活会因为借不到
    Key 而显示成鉴权错误，容易被误读成网关有问题。这两个字段目前都是空的占位，
    等拿到真实的直连地址和探活 Key 之后由网关这边的同学自己填进 `gateway_profile.json`，
    不通过对话传递。

11. **"客户端里什么都没配"被一律当成"配错了"来报。** 真实反馈的一台机器：`settings.json`
    存在但里面没有任何网关配置，Suture 于是逐项报"没配置网关地址/没配置鉴权信息/
    没配置模型名称"，还把网关全部 21 个型号平铺出来——看上去满屏报错，但这个人的
    客户端一直能正常使用（连接是在客户端之外解决的，客户端本来就不需要填这些）。
    根因是判断标准错了：Suture 拿"字段有没有值"当作"能不能用"的依据，而这个前提
    只对一部分用户成立。改了两处：
    - 不填模型名不再算问题（客户端不填会用它自己的默认模型），型号清单收进折叠区；
      只有端到端确实没跑通、又没指定模型时才升级成需要处理的问题。
    - 地址和鉴权都没填、Suture 自己发不出验证请求时，改成调用客户端本体跑一次最小的
      非交互请求（Claude Code 是 `claude -p`），让它用**它自己**解析出来的配置和凭据
      说话：跑通了就说明这台机器现在能用，那几项不报成故障、也不给一键修复按钮
      （避免去改一套正在工作的配置）；跑不通才是真的没配置。连配置文件都没有的机器
      同样先自证，避免对着一台正常工作的机器提示"帮你生成一份配置"——那份配置会
      盖掉它现在能用的那条路。

12. **图形界面的交互设计本身也有问题，不只是文案。** 走查实际渲染出来的报告页面后
    发现三处：「开始检查」和「重新检查」是两个独立按钮，绑的是同一个函数，检查完之后
    两个同时可见，看起来像两个功能；顶部状态条的主标题/主详情被直接塞进了阶段一网关
    探活的原始技术细节（延迟、状态码），跟这次检查真正的结论（有没有问题、需不需要
    处理）混在一起，容易读成"阶段一的探测结果就是问题本身"；模型候选清单用的是
    并排的一排按钮，网关型号一多（这次是 21 个）就变成占满屏幕的按钮墙。改成：
    合并成一个按钮，检查完把文案从「开始检查」换成「重新检查」；状态条拆成两行，
    主标题/主详情只呈现整体结论，阶段一的原始探测详情降级放到一行更小的次要文字里；
    候选清单改成有滚动条、限定高度的纵向列表，一行一个候选，不再横向铺开。
    过程中还顺带发现一个之前没暴露的问题：`report.message` 在"发现可自动修复的问题"
    这一种结果分支下一直没被赋值，只是因为原来的状态条会强行拼一段阶段一探测文字垫底
    才没露出来——把阶段一详情挪走之后这条分支的主详情会变成空白，一并把这条分支的
    结论文案补上。

13. **「帮我生成一份配置」成功提示里的换行符不会真的换行。** 提示文案里用 `\n`
    分隔文件路径和后面的说明，但展示这段文字的容器没有对应的换行样式，浏览器
    会把换行符当空白折叠掉，两句话挤成一行——文件路径和"请将其中的 Key 替换…"
    连在一起，不容易读。给这个容器补上保留换行的样式。
14. **这个按钮失败之后没法重试。** 点击后先把按钮禁用，失败时只展示了一条红色
    错误提示，没有把按钮重新启用、文案也没有恢复——只要生成失败一次（目录没
    权限、磁盘满等），这个按钮就永久卡死，唯一的出路是刷新整个页面重新走一遍
    检查流程。同一份代码里「一键修复」「应用选择」两处失败时都正确恢复了按钮，
    只有这一处漏了。改成失败时也恢复按钮可点状态和原文案，并在处理中给出
    「正在生成…」的文字反馈，跟另外两处保持一致。

15. **阶段一挂在页面最顶上、跟下面阶段二的卡片列表分开摆，看起来像两份不相关的
    东西。** 上一轮把阶段一的探测细节从主状态条里挪到了一行更小的次要文字，但
    位置还是留在最顶上，跟下面阶段二的卡片之间隔着按钮，视觉上仍然是断开的两块。
    改成把这一行从状态条里搬出来，紧挨着放在卡片列表上方——顶部状态条只讲这次
    检查的结论，下面从阶段一这一行开始到每个客户端的卡片，连成一段完整的"过程"，
    不再各占一头。这一行还加了跟卡片其它地方一致的圆点配色（网关本身没问题是绿色，
    鉴权被拒绝这类不阻塞阶段二的情况是黄色，网关侧问题阻塞阶段二时是红色）。

16. **鉴权 401 的归因分不清「Key 放错请求头」和「Key 真失效」，且真实主因会被
    无关静态问题挤掉。** 2026-09 用真实网关（tower-ai）实测确认：同样一把有效 Key
    放 x-api-key / Authorization 一律 401，放自定义 Token 头才 200——AI Gate 实际
    只从 Token 头取 Key。之前 401 一律兜底成「Key 被吊销，重新生成」，对 Key 有效、
    只是没放进 Token 头的用户是假诊断（DeepSeek 之前就是因为适配器把 Key 放
    Authorization 而被一直误报「填 Key」）。改成 profile 契约驱动，网关变了只改
    JSON 不动代码：
      · gateway_profile.json 的 auth.required_header 声明网关采信哪个头（现为 Token）；
      · 401 且 Key 前缀合法、但该 Key 没被放进 required_header → 给「一键补请求头」
        （deepseek 写 providers.<route>.headers.Token；claude_code 写
        ANTHROPIC_CUSTOM_HEADERS 里的 Token 行），不再直接让用户重新生成 Key；
      · Key 前缀不对、或补头后仍 401 → 才归成「填 Key / Key 失效」；
      · blocked_reasons 现在保证 e2e 的真实失败方向（鉴权/模型/网关侧）一定出现在
        原因清单里，不会被无关静态问题挤掉；
      · 多候选模型全失败时，取能代表根因的那一次（鉴权失败优先）做归因。
    新增 tests/test_auth_header.py，用 behavior=token_only 的假网关（只认 Token 头）
    验收以上分支。

17. **Codex 对「只认自定义请求头」的网关是契约层面不可达，之前会把它带去填 Key /
    补头的死循环。** 2026-09-09 在真实网关（tower-ai）做「头隔离」实测钉死：
    anthropic（`/v1/messages`）和 openai（`/v1/chat/completions`）两种形态都
    只从自定义 `Token` 请求头取 Key，x-api-key / Authorization 一律 401
    「个人apikey认证失败」；OpenAI 形态路由真实存在、+Token 返回 200（Codex 的
    base_url 拼 root+`/v1`+`/chat/completions` 是对的）。但 Codex CLI 没有任何
    可附加自定义请求头的位置，只会把 Key 放进 `Authorization: Bearer` → 即使
    Key 正确也永远 401。之前对 codex 的 401 会给出「补 Token 头」外部指引或
    「填 Key」输入——前者 codex 无从执行、后者填了也白填。改成契约驱动的判定：
    `auth.required_header` 不在该客户端会发的请求头集合、且该客户端没有能力附加
    自定义请求头（适配器 `can_send_custom_request_headers=False`，目前只有 codex）
    → blocked 只给单条 external `auth-incompatible`（说明改用 Claude Code /
    DeepSeek，或等网关放开其它鉴权头），`configure_client` 对这类客户端直接拒绝、
    不生成配置也不写环境变量。判定不写死客户端名：哪天网关把 `required_header`
    改成 Authorization（codex 本来就会发的头），同一个 codex 自动变可修。
    新增 tests/test_codex_contract.py 验收。

## 2026-09-09（同日）三路专项审查 + 修复

对「可达性 / UI 可读性 / 跨模块一致与安全」做了三路只读审查，逐条对照源码核验后确认并修掉
的问题（新增 `tests/test_review_fixes.py`，并对两处受语义影响的既有断言做了更新）：

| 类别 | 确认的问题 | 修法 |
|---|---|---|
| 可达性 | codex 缺 base_url（没发过 e2e）时绕过「外部处理」、先给 auto 修地址——配完才冒出 401 | 契约不可达短路与 e2e 解耦：hint≠网关侧即给单条 `auth-incompatible` 外部说明 |
| 可达性 | 生效值来自系统环境变量（claude 的 ANTHROPIC_BASE_URL/MODEL）时，auto 写配置不生效；conflict 的 auto 还会把错误值复制回文件——两条按钮互相否定、原地循环 | blocked 阶段统一把这类 base_url/model 问题换成一条「去系统环境变量改/删」的外部说明，去掉 conflict 与 fallback 的自动修复 |
| 可达性 | 型号在快照里但网关实际 400/404、且地址本就正确 → 给「改成规范地址」的 no-op 循环 | 地址正确时改给 `model-unrouted` 外部说明 |
| 可达性 | `/api/action` 不回写 `last_report` → 修复暴露的下一层 issue 一点必 409，多步修复链断 | action 成功后把最新 client 写回 last_report（与 recheck/configure 对齐） |
| 一致/安全 | `probe_key` 被硬编码塞 x-api-key，而网关只认 profile 声明的头 | 探活按 `auth.required_header` 放 Key |
| 一致/安全 | claude_code 写出的含 Key 配置文件不收紧权限 | 写后 chmod 600，与 deepseek / 备份文件口径一致 |
| 一致/安全 | store_key / apply 写失败时报「原配置未改动」不实（可能已部分写入） | 失败分支真调用 `fixer.rollback` 并按结果如实措辞 |
| 一致/安全 | `configure_client` 对已有配置整文件覆盖且无备份 | 已有配置时拒绝重配（只服务「装了但一行没配」），引导回检查页按原因修 |
| 一致/安全 | `detect_binary` 不收 engine.home，按目录判装的基准与 read 不一致 | 签名加 `home` 参数并全链路传透 |
| UI | 空 Key 点保存崩出英文 TypeError、按钮变「重试」；后端 manual 响应不带 client 时前端重建卡片崩溃 | 本地中文拦截 + manual 响应只追加提示、不重建卡片、按钮恢复原文案 |
| UI | 安装命令失败/缺 Node 的错误输出被随后的 `renderInstall` 整列表清空，用户看不到原因 | 先 reloadState+renderInstall 再写输出区与结果横幅 |
| UI | 安装页用启动时缓存、无刷新入口；DeepSeek 引导里引用的「重新探测」按钮不存在 | 切到安装页时重新探测；引导文案改为真实存在的操作 |
| UI | 单卡修绿后顶部总览仍旧是上一轮「部分无法连接」 | 按当前卡片状态就地重算总览（`recomputeOverview`） |
| UI | 配置文件语法错这类 `none` 项没有任何「下一步」 | 给 none 项补「按提示处理完后点开始检查」微文案 |

**评审明确「不改语义、文档化」的边界**（供产品侧再定，勿当漏网）：检查页「有配置即走真实
请求」、安装页按可执行文件判装——配置存在但 PATH 找不到可执行文件时两页口径不同属预期
（deepseek 已按目录判装对齐；claude/codex 的 PATH 探测边界保留）；「未指定模型时用探活模型
证明传输/鉴权通即判 connected」也保留——推翻它等于退回到要求人人配模型的旧语义。

**「能通就算能连」是产品已确认的判定口径（2026-09-10 口头确认）**：判定只看真实请求能不能
通，**不校验请求地址是否在网关登记地址（`canonical_root` / `alternate_roots`）之内**。所以
客户端把地址指向一个本地转发/HTTP 兼容服务、请求能拿到 200 时，判 `connected`、显示
「已连接 AI Gate。」，**不加任何「这不是网关登记地址」的警告**。实测触发例：本机 codex 的
`base_url=http://127.0.0.1:15888`（一个对任何请求都回 200 的本地 OpenAI 兼容服务，无需
鉴权、也无 Key 可解析）→ 判 connected。这是**预期行为**（口径是「能不能用」而非「是不是
直连登记地址」），下一轮审查不要再当 bug 报。

---

## 2026-09-10 第二轮专项审查 + 修复（P0/P1 五条）

对 `main`（`77a4474`）再做一轮只读审查（判定/可达性/适配器 与 界面/服务端 两路并行），
逐条对照源码核验并在沙箱里复现。确认 7 条缺陷 + 5 条体验问题，其中 **P0/P1 六条已修**
（问题 6 与 1 同属一条链路，顺手一并改掉），全部新增回归验收见
`tests/test_review_fixes.py`（13 条）。修复后的全量：**109 条全过，跳过 1**。
完整清单与未修项见仓库根目录 `REVIEW-2026-09-10.md`。

| 类别 | 确认的问题 | 修法 |
|---|---|---|
| 安全 | issue 的 `fix_value` 带着**原始 Key**（补 Token 头、去掉首尾空白这两条修复的载荷）经 `report_to_dict` 直达浏览器；`e2e.detail` 还会拼上 urllib 异常原文（`Invalid header value b'yotta_pk_...\n'`） | `report_to_dict()` 是唯一出网口，就地删掉每个 issue 的 `fix_value`（界面从不读它，点「修复」只发 `{client_id, issue_id}`，值由服务端从自己那份 `last_report` 取）；`gateway` 侧异常不再外传原文，只留异常类型名 |
| 安全 | `os.chmod(0o600)` 在 Windows 上是空操作（实测 `S_IMODE == 0o666`），文档承诺的「权限收紧」在 Windows 不成立 | **未修**，属文档口径问题（见 REVIEW 第 2 条），待定 |
| 数据丢失 | 目标配置文件有语法错时 `apply()` 按空 data 起草再整文件写回，「修一个地址」把同文件的 `permissions` / `hooks` / `mcpServers` 静默抹掉，还报「已修改」 | 新增 `RefuseWrite` + 适配器 `unparseable_write_target()`：目标文件读不懂就**拒绝写入**，判定层也不给「修复」按钮（转 `external`）；引擎捕获后如实说「没有修改配置」，原文件逐字节不动 |
| 崩溃 | 三个适配器读配置只接 `OSError` 和各解析错误，非 UTF-8（记事本存成 ANSI/GBK）抛出的 `UnicodeDecodeError` 不是它们的子类 → 整轮检查抛出去；`/api/check` 又是唯一没有 try/except 的路由，界面只剩「检查失败」 | 三处读取（claude json / codex toml / deepseek yaml）、deepseek 的 `.env` 读取与凭据文件读取统一接住，转成可展示的「文件不是 UTF-8 编码，请另存为 UTF-8」；codex `apply` 读不出编码时同样拒绝写 |
| 假诊断 | Key 带换行等「请求在本机就没构造成功」的情况被 `e2e_hint` 归成 unknown，兜底给出一条 **high** 的「网关侧连不上…联系网关值班人员」，顶在清单第一条，压过真正能修的「Key 前后有多余空格/换行」 | 新增 `local_error` 分类（`http.client.InvalidURL` / `ValueError` / `URLError` 里的 `unknown url type`）：本机构造失败单独成一条 medium 说明「请求没能发出去，先处理下面的本地问题」，不再指向网关 |
| 无效修复 | 生效值来自**组织托管配置**（只读、优先级最高）时 `base_url`/`model` 仍给 auto——写的是被压过的那一层，点了不生效、原因原样回来（`env_wrong` 短路只判了「环境变量」） | 「写不进去的生效层」统一识别（环境变量 **或** managed），换成一条 `readonly-source` 外部说明（文案区分两种来源：去系统环境变量改 / 找 IT），并清掉所有指向该字段的 issue（含 conflict 与 fallback 里的 no-op 自动修复） |
| 无效修复 | 「多层配置取值不一致」的 auto 写的就是生效层（本来就是这个值）= 原样重写一遍，同一条原因永远清不掉，且与 `base_url` 那条的写入值互相矛盾 | 改成 `FIXABLE_NO` / `none`，文案说明「请把其它几层里这一项删掉或改成同一个值」——真要自动统一得实现「删其它层的键」，是另一套动作，这轮不做 |

**这一轮明确没动的**：能通就算能连（不校验是否网关登记地址）、安装页/检查页判装口径差异、
未指定模型时用探活模型证明传输/鉴权通即判 connected、codex 契约不可达——都是已确认口径。
Windows 的 chmod 空操作、以及 5 条体验类问题（configure 的 note 不渲染、安装页并发、
英文报错与静默失败、安装失败无输出区、检查无进度）保持原样，已记进
`REVIEW-2026-09-10.md` 待定。

## 还没验证的

- 六个平台里只在 Linux x64 上实际打包并运行过；其余五个需要在对应机器上跑一次构建。
- 客户端自证目前对 Claude Code（`claude -p`）和 Codex CLI（`codex exec
  --skip-git-repo-check`，已对照 developers.openai.com/codex/noninteractive 核实）
  生效。DeepSeek Harness 的非交互调用方式还没确认，`self_test_command()` 返回
  None、直接跳过自证——所以这个客户端在"什么都没配但其实能用"的机器上仍然会报
  "没配置"。确认了正确命令之后补上即可，不需要改其它逻辑。
- 代码签名与公证尚未接入，证书就绪后需要补上并重新验证 macOS/Windows 的打开流程。
- Codex 的 `base_url` 带 `/v1` 后缀已实测确认：真实网关 OpenAI 形态路由
  `/v1/chat/completions` 存在、带 Token 头返回 200，root + `/v1` + `/chat/completions`
  的拼法与网关一致。
- 「端到端连通即代表所有模型都可用」这个假设依赖网关对同一个 Key 的所有模型一视同仁，
  没有按模型单独限权限；如果网关实际是按 Key 分模型权限的，需要重新评估。
- AI Gate「只从 Token 头取 Key」是按 tower-ai 网关「头隔离」实测写进
  gateway_profile.json 的 auth.required_header，anthropic 与 openai 两种形态都验证过。
  若网关改成也认别的头，更新该字段判定即跟着变。Codex 不能在请求里带自定义 Token 头
  （无此机制，已实测定性）→ 判定走「外部处理」，见上方第 17 条。
