# Suture 程序设计说明

这份文档面向研发，讲清楚 Suture 这个程序内部怎么分层、界面怎么承载、六个平台怎么构建分发。
产品侧"检查什么、怎么判断、修复到什么程度"在产品方案文档里，这里不重复，只在需要对应的
地方引用。

> 2026-09 重构为「连通优先」：每个客户端只判能不能连上 AI Gate（四态：连通 / 连不上 /
> 没配置 / 没安装）。通就绿勾、不罗列任何细项；连不上才展开"可能的原因"，每条=人话+
> 怎么修，用户挑着处理；另加「安装 / 上手」页自动装缺失客户端。细节见 §3 与
> `VERIFICATION.md` 末尾的验收段。

---

## 1. 整体架构

程序分五层，从下往上依次是规则、配置读取、检测、网关交互、修复/安装，界面层横跨在最上面。
分层的目的是让"判断逻辑"和"界面"彻底分开：命令行和图形界面调用的是同一套引擎，不存在
两份判断逻辑。

```
              ┌──────────────────────────────┐
界面层         │  图形界面（WebView / 浏览器）  │  命令行
              └───────────────┬──────────────┘
                              │  同一套引擎接口
              ┌───────────────▼──────────────┐
编排层         │  run(): 网关探活 → 逐客户端四态 │
              └───┬────────┬────────┬────────┘
                  │        │        │
        ┌─────────▼──┐ ┌───▼────┐ ┌─▼─────────┐
能力层   │ 网关交互    │ │ 检测   │ │ 修复/安装  │
        └─────────┬──┘ └───┬────┘ └─┬─────────┘
                  │        │        │
              ┌───▼────────▼────────▼────┐
基础层         │ 配置读取（按 harness 分流） │
              │ 网关规则（外置 profile）    │
              └──────────────────────────┘
```

数据流是单向的：配置读取产出一份"实际生效值 + 每个值来自哪一层"的结构，检测层只对这份
结构做判断并产出问题清单，修复层只消费问题清单并写回文件，网关交互层负责所有出网请求。
检测层不碰文件写入，修复层不做判断，这条边界要守住——之前设计上出过的几次反复，根源都是
判断和动作混在了一起。

连通优先模式下，静态检测（`checks.py`）从"唯一判断手段"降级为**连不上时才会跑的诊断器**：
成功路径不调用它，只有 `blocked` 分支才把 not-ok 的 finding 翻译成给用户挑着修的原因清单。

---

## 2. 模块划分

| 模块 | 职责 | 状态 |
|---|---|---|
| `suture/profile.py` | 加载网关规则文件；预留运行时从网关拉取规则的接口 | 已有 |
| `suture/harness/` | 三个客户端各一个适配器：读取/归一化配置、命令探测、官方安装命令、Key 落点 | 已有 |
| `suture/checks.py` | 静态检测项 + 连不上时翻译成"可能的原因"的诊断助手 | 已有 |
| `suture/gateway.py` | 网关探活、发真实请求、响应分类 | 已有 |
| `suture/selftest.py` | 让客户端用它**自己**的配置自证能否使用（进程输出以文件承接，杜绝挂死） | 已有 |
| `suture/fixer.py` | 备份、写入 | 已有 |
| `suture/installer.py` | 探测 Node 运行时、执行官方安装命令并回收输出（300s 超时 + 进程树强杀） | 已有 |
| `suture/engine.py` | 单客户端四态判定、逐条修复动作、一键配置、整体编排 | 已有 |
| `suture/cli.py` | 命令行编排 | 已有 |
| `suture/server.py` | 本地 HTTP 服务：state/check/action/configure/recheck/install_run | 已有 |
| `suture/shell.py` | 启动窗口（WebView 优先，浏览器兜底） | 已有 |
| `suture/ui/index.html` | 界面（自包含，两个页签） | 已有 |

### harness 适配器

三个 harness 的配置载体、字段名、优先级规则都不一样，这部分必须按 harness 分流，不能共用一套字段名假设。每个适配器对外暴露同一组能力：探测本机是否安装了这个 harness、读取它的配置、把修正值写回它的配置。

```
suture/harness/
    base.py            # 适配器接口定义
    claude_code.py     # 环境变量 + 全局/项目级 settings.json
    codex.py           # ~/.codex/config.toml + 项目级 .codex/config.toml
    deepseek.py        # cordis.yml 基线 + settings.yaml 覆盖 + .credentials.yaml
```

三个适配器各自要处理的特殊结构：

- **claude_code**：鉴权值直接存在配置里，两个环境变量对应两个不同请求头。
- **codex**：`env_key` 是间接引用，存的是环境变量名而不是值，需要多一步"这个名字对应的环境变量存不存在、有没有值"的检查；项目级配置只在项目被标记为可信时才生效，读取时要把这个前提考虑进去，否则会对着一份不生效的文件做修复。**Codex 没有可附加自定义请求头的位置**（只会把 Key 放进 `Authorization: Bearer`），所以对「只认某个自定义头」的网关（AI Gate 的 `Token`）是契约层面不可达的——判定层把它的鉴权失败归成「外部处理」，不给填 Key / 补头引导（见 §7）。
- **deepseek**：配置分散在三个文件里，覆盖方向与另外两个相反（用户层覆盖项目基线）；
  Key 有独立的四层优先级；鉴权是间接引用（`apiKeyEnv`）。AI Gate 只从自定义
  `Token` 请求头读 Key，而 dsh 把额外请求头写在路由的 `headers.Token` 里——所以
  read 时要把 `headers.Token` 当「平行额外头」一起暴露，写 Key（store_key / 自动补头）
  时也要把 Key 写进这个头，而不是只写凭据文件。

---

## 3. 界面方案

### 3.1 承载方式：WebView 优先，浏览器兜底

目标是"双击一个文件，出来一个窗口"。三个系统上系统自带的 WebView 情况不同：

| 系统 | 系统 WebView | 是否需要用户额外安装 |
|---|---|---|
| macOS | WKWebView，系统自带 | 不需要 |
| Windows | WebView2，Windows 11 预装，Windows 10 通过系统更新推送 | 绝大多数情况不需要 |
| Linux | WebKitGTK，不随系统预装 | 需要通过包管理器安装 |

因此采用两级方案：程序启动时先尝试初始化系统 WebView，成功就弹出一个应用窗口；初始化失败（主要是 Linux 缺 WebKitGTK 的情况）就退回到"起本地服务 + 打开系统默认浏览器"，界面内容完全一样，只是承载的容器不同。

这样处理的理由是：非技术用户集中在 Windows 和 macOS，这两个平台上能拿到真正的应用窗口体验，并且不需要额外装任何东西；Linux 上的用户具备自行安装依赖的能力，即使不装也仍然能用，不会出现"打开就报错、什么都做不了"的情况。

无论走哪条路径，界面本身都是同一套 HTML/CSS/JS，通过本地 HTTP 接口和引擎通信。服务只监听 127.0.0.1，端口随机分配，启动时生成一次性令牌，界面请求必须带上这个令牌——避免本机其他程序能调用这个接口去读配置。

### 3.2 视觉规范

参照 Claude 的设计语言。字体使用系统字体栈，配置值、字段名、地址这类内容使用等宽字体，与正文区分开。

```css
:root {
  --bg:            #FAF9F5;   /* 暖白底 */
  --surface:       #FFFFFF;
  --surface-alt:   #F0EEE6;
  --ink:           #141413;
  --ink-muted:     #6B6B68;
  --border:        #E8E6DE;
  --accent:        #D97757;   /* Claude 赤陶色，用于主按钮和强调 */
  --accent-soft:   #F7E9E2;
  --success:       #2C7A55;
  --success-soft:  #E3F1E9;
  --warning:       #9A6B1F;
  --warning-soft:  #F7EDD9;
  --danger:        #B3261E;
  --danger-soft:   #FBE7E5;

  --font-ui:   -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
               "Microsoft YaHei", sans-serif;
  --font-mono: ui-monospace, "SF Mono", Menlo, Consolas, monospace;

  --radius-card: 12px;
  --radius-ctl:  8px;
  --radius-pill: 999px;
}
```

深色模式提供一套对应的 token，跟随系统设置切换。

组件清单，只需要这些，不必更多：

| 组件 | 用途 |
|---|---|
| 页签导航 | 「检查」和「安装 / 上手」两个页 |
| 主按钮 | 「开始检查」，固定文案、检查完不改成别的名字（不再有「重新检查」按钮） |
| 状态条 | 顶部一行，只显示这次检查的整体结论 |
| 客户端卡片 | 每客户端一行：`connected` 只绿勾 ✓；`blocked` 展开「可能的原因」；`unconfigured` 给配置面板入口；`not_installed` 给去安装页的入口 |
| 原因行 | blocked 才出现：标题 + 一句解释 + 对应控件 |
| 修复控件 | 按原因类型给：`修复` 按钮 / 模型候选下拉 / Key 输入框 / 外部处理只给说明 |
| 配置面板 | unconfigured 客户端：默认模型下拉（取自网关路由表）+ Key 输入框 + 保存并测试 |
| 安装卡 | 安装页每客户端：已装/未装徽标 + 官方命令只读回显 + 「运行安装命令」+ 等宽输出区 |
| 结果提示 | 单条动作 / 配置之后的收尾消息（成功绿 / 仍需处理黄 / 失败红） |

界面上任何位置展示 Key 都必须是掩码后的形式，只露前 4 位和后 4 位。Key 的输入框用
密码态，提交后不把明文回显。

### 3.3 界面状态

界面按「检查」「安装 / 上手」两个页组织。「检查」页只有一个主按钮，始终叫「开始检查」；
点一次跑完整轮检查，结果按客户端四态渲染，不把同一函数绑成两个名字不同的按钮。
「安装 / 上手」页列出三个客户端，未装且能自动装（有官方命令）的给「运行安装命令」，
装完回「检查」页测连接。

单个客户端的四态渲染：

```
connected（绿勾 ✓，不展开任何细节）
blocked（展开「可能的原因」→ 挑一条 → 修复 / 候选 / Key / 外部）
unconfigured（[配置] → 默认模型 + Key → 保存并测试）
not_installed（去「安装 / 上手」页）
```

---

## 4. 跨平台与构建

### 4.1 六种目标组合

三个系统各两种 CPU 架构，六种全部支持。对应的 GitHub Actions 构建机器：

| 目标 | 构建用 runner |
|---|---|
| Windows x64 | `windows-latest` |
| Windows ARM64 | `windows-11-arm` |
| macOS Apple Silicon | `macos-latest` |
| macOS Intel | `macos-15-intel` |
| Linux x64 | `ubuntu-22.04` |
| Linux ARM64 | `ubuntu-22.04-arm` |

### 4.2 打包工具的约束

使用 PyInstaller 打包成单文件可执行程序。有两条硬约束需要在设计构建流水线时考虑：

**不能跨系统交叉编译。** 不存在"在 Linux 上打出 Windows 的 exe"这种做法，每个目标必须在对应系统的机器上构建。这就是上面那张表要列六台机器的原因，不是流程繁琐，是工具本身的限制。

**macOS 可以合并成一个文件。** PyInstaller 的 `--target-arch` 支持 `x86_64`、`arm64`、`universal2` 三种取值，在 universal2 的 Python 环境下可以打出同时包含两种架构的单个文件。如果采用这种方式，macOS 侧对外只需要发一个文件而不是两个，用户不用自己判断该下载哪一个。代价是需要保证依赖也都提供 universal2 版本——Suture 只用标准库，这个前提是满足的。建议采用，但仍保留分架构构建作为兜底。

### 4.3 Linux 的 glibc 兼容性

在较新的发行版上构建出来的二进制，拿到较老的系统上会因为 glibc 版本不匹配而无法运行，反过来则没有问题。因此 Linux 侧固定在 `ubuntu-22.04` 上构建，而不是跟着 `ubuntu-latest` 走，保证产物能在更新的发行版上运行。

---

## 5. 代码签名与分发

三个平台对未签名程序的处理方式差别很大，这直接影响非技术用户能不能顺利打开：

| 平台 | 不签名的后果 | 需要准备什么 |
|---|---|---|
| macOS | Gatekeeper 直接拦截，提示"无法验证开发者"，普通用户打不开 | Apple Developer ID 证书，签名后还要做公证并把公证结果附到文件上 |
| Windows | SmartScreen 弹出"已保护你的电脑"警告，用户需要点两次才能继续 | Authenticode 代码签名证书 |
| Linux | 无系统级拦截 | 发布 SHA256 校验值，需要的话再附 GPG 签名 |

macOS 这条是硬门槛，不做签名和公证，非技术用户基本上装不上。Windows 的警告虽然可以绕过，但对这份工具的定位（要读取用户的 Key）来说，一个会弹安全警告的程序很难建立信任，建议一并处理。

分发方式：内部下载页按系统和架构提供对应文件，页面上同时给出校验值。程序每次启动时校验自身版本号，发现有新版本时提示更新——这一点同时也是网关规则更新机制的兜底，见下一节。

---

## 6. 网关规则的外置

模型路由表、地址格式要求、鉴权方式这些信息会随网关演进而变化，不能编译进二进制。规则以独立的 JSON 文件形式存在，程序启动时按以下顺序取用：

1. 命令行参数指定的规则文件（研发调试用）
2. 从网关运行时拉取的最新规则（正式方案，接口已在 `profile.py` 里预留）
3. 二进制内置的一份快照（兜底，同时用于离线场景）

走到第 3 条时界面需要提示"当前使用的是内置规则，可能不是最新的"，避免用一份过期的路由表去判断用户的模型名填得对不对，把本来正确的配置改错。

---

## 7. 关键设计决策

| 决策 | 依据 |
|---|---|
| 引擎只用 Python 标准库 | 依赖越少，六个平台的打包越不容易出问题；这个工具的逻辑本身不需要第三方库 |
| 界面走 WebView + 本地服务，而不是原生 GUI 框架 | 一套 HTML/CSS 覆盖六个平台，视觉上也更容易贴合 Claude 的设计语言；原生框架要为每个平台单独调样式 |
| 判断逻辑与界面彻底分离 | 命令行与图形界面共用同一套引擎，避免两处逻辑各自演化后出现结论不一致 |
| 网关规则外置而非硬编码 | 网关新增模型、调整协议时，不需要重新分发二进制 |
| 客户端数据的脱敏收成唯一出口 `redact_client()` | 返回客户端数据的路由有四条（`/api/check`、`/api/action`、`/api/configure`、`/api/recheck`），`fix_value` 可能是原始 Key（补 Token 头、去掉首尾空白两条修复的载荷）。只在其中一条上删字段等于没堵——上一轮就是这么漏的。现在四条路共用同一个函数；服务端自己那份 `last_report` 保留原值（点「修复」要用它）。界面从不读出网副本里的 `fix_value`：点「修复」只发 `{client_id, issue_id}`，值由服务端从自己那份 `last_report` 里取 |
| 配置文件读不懂时**拒绝写入**，而不是按空数据整文件写回 | 解析失败后 data 是空的，按它起草再写回 = 用一份只含本次改动的文件覆盖原文件，同一份 settings.json 里的 permissions / hooks 会被静默抹掉。宁可写不了、如实报错，也不能悄悄清空别人机器上的其它配置；判定层同步不给「修复」按钮（`RefuseWrite` + `unparseable_write_target()`）。这条对所有会**整文件重写**的目标统一适用：deepseek 的 `settings.yaml` **和 `.credentials.yaml`**（后者的 `refs` 段是多厂商共用的，读不懂照写会把别人的 Key 一起覆盖掉）、codex 的 `config.toml`（`except OSError` 原先降级成「文件是空的」，同样会整份重写） |
| 写出器与读取器必须能往返，写不出来的值明确报错 | 受限的 YAML 写出器若产出自己读不回来的文件，下次读取会把整份配置判成语法错 → 触发「读不懂就不写」→ 用户被自己写坏的文件永久卡住。两处收口：空集合 `{}` / `[]` 没有歧义，解析器放行（其余流式写法照旧拒绝）；值里有换行时**明确报错**（YAML 里换行必须用块标量，这个写出器不支持），并提示多半是复制 Key 时把换行一起粘了进来。配套：auto/choice 写鉴权字段前先 `strip()`，否则等于把「Key 末尾有换行」这个病原封不动搬进新文件 |
| 「生效层写不进去」统一识别，不给自动修复 | 环境变量与组织托管配置都属这类：auto 写的是被压过的那一层，点了不生效、原因原样回来（conflict 那条更糟，会把只读层的值复制回文件）。判定层按「是否可写」而非按来源名字判断——三个适配器给环境变量层的命名各不相同（`环境变量` / `环境变量 AI_GATE_API_KEY` / `启动时的环境变量` / 两种 `.env`），按名字精确匹配必漏；`_cannot_write(cfg, keys)` 统一收口，将来新增只读来源不用再改一遍 |
| 请求的**构造**也在 `try` 里，本机失败同样归因 | `urllib` 的 `Request(...)` 在构造时就会因 URL 少 `https://` 抛 `ValueError`、因鉴权值带换行抛 `http.client.InvalidURL`。写在 `try` 外，异常就绕过 `_classify` 直接抛给调用方——整轮检查崩掉、界面只剩英文 "Failed to fetch"，而这两类恰好都有专门的中文说明等着用。凡是「本机就没把这次请求拼出来」的失败，都要走到和其它失败同一个归因出口 |
| 执行外部命令前先把命令名解析成真实路径 | Windows 的 `CreateProcess` 不按 `PATHEXT` 补扩展名：npm 只有 `npm.cmd`，`shutil.which` 找得到、`Popen(["npm", ...])` 却必抛 WinError 2。探测与执行必须同一口径（`shutil.which` 解析后再执行），解析不到就给中文指引，绝不把 WinError 抛给目标用户 |
| 本地服务绑定 127.0.0.1 并带一次性令牌 | 这个接口能读到配置里的 Key，不能让本机其他程序随便调用 |
| 连通优先：真实请求通了就判 `connected`，不再逐项静态体检 | 非技术用户要的是"能不能用"，不是"哪里跟规范不一致"；成功用户把几十项细项平铺出来只会造成满屏报错 |
| 静态检测只在连不上时当诊断器跑 | 把它从"唯一判断手段"降级成归因工具，避免健康配置被打成"有问题、可自动修" |
| 鉴权 401 先区分「Key 没放进网关要求的头」vs「Key 真失效」 | 网关契约（profile 里 `auth.required_header`）说清网关从哪个请求头取 Key。401 且 Key 前缀合法、但配置没把这个 Key 放进那个头时，给「一键补请求头」而不是误导用户重新生成 Key；补了头仍 401（或 Key 前缀本身不对）才归成 Key 失效。契约随网关演进而变，只改 profile 不动代码 |
| 连不上时 e2e 的真实失败方向（鉴权/模型/网关侧）必须出现在原因清单里 | 静态检查会吐一些无关紧要的问题（旧字段、拼写等），不能因为它们存在就让真正的 401/5xx 主因被挤掉 |
| 客户端带不了网关要求的自定义请求头时直接给「外部处理」（codex ↔ AI Gate 的 Token） | Codex 没有附加自定义请求头的位置、只会发 Authorization，而网关只认 Token → 填 Key / 补头都是无效引导，只会让用户对着 401 反复折腾。判定契约驱动（profile.auth.required_header × 适配器 can_send_custom_request_headers），不写死客户端名；哪天网关改认 Authorization，同一个 codex 就自动变可修，不用改代码 |
| 生效值来自系统环境变量、而该客户端写不到它时只给外部指引（claude 的 `ANTHROPIC_BASE_URL` / `ANTHROPIC_MODEL` 是 OS 环境变量，优先级压过配置文件） | 自动修复写的是低优先级那一层、点了不生效；conflict 的自动修复还会把环境变量里的（错误）值复制回文件，两条按钮互相否定、原地循环 → 这类 base_url/model 问题在连不上时统一给一条「去系统环境变量里改/删」的外部说明，去掉所有会写文件的自动修复 |
| 多候选模型都失败时，取能代表根因的那一次做归因 | 只记最后一次失败会把更早的鉴权失败掩盖掉，导致归因方向错 |
| 决定性请求用客户端自己配的模型发 | 模型名配错要能被如实抓到，不能拿网关保证认识的探活模型替代、掩盖成已连通 |
| 连不上给出的原因按修复方式分类（自动/候选/Key/外部） | 「一键修复 N 项」表意不清：有的原因能安全自动改，有的要选、要粘贴、或只能在工具外做，得让用户看得懂每一条、挑着修 |
| 单条修复 = 备份 → 写入 → 用新配置重测，不自动回滚 | 一次只改一个逻辑字段、写完立刻实测，改动小且可预期；备份保留给人工找回。多条原因并行存在时逐条处理，不再整段"修完网关恰好挂了就保留/网关正常就不回滚"地赌 |
| 修网关地址时探的是规则里登记的正确地址 | 用户把地址填错时，不能把"连不上"归给网关、放着真正的错不修 |
| 修复前必须备份，备份文件收紧权限 | 任何写回都有原始文件可找回；备份文件里同样有 Key 明文，权限不收紧等于多留了一份泄露面 |
| 客户端自证/安装命令的进程输出用临时文件承接，超时强杀进程树 | npm/客户端会派生孙进程；用管道承接时孙进程占着写端，父进程收尾会被永久拖住（Windows 上实测过 90s+ 不返回）。等"进程退出"而不是"管道关闭"就不会挂死 |
| 安装只执行适配器写死的官方命令 + 测试覆盖开关 | 安装会真实往系统里装东西，绝不接收用户任意输入的命令；DeepSeek 官方命令没确认前只给引导，不跑没验证过的命令 |

---

## 8. 待验证事项

以下几点在实现前需要落实，目前按最可能的情况先写着：

- Codex CLI 的 `base_url` 带 `/v1` 后缀：已实测确认（真实网关的 OpenAI 形态路由
  `/v1/chat/completions` 存在、带 Token 头返回 200），拼法正确。仍待网关或 Codex
  某一方松绑：网关放开对其它鉴权头的采信、或 Codex 后续版本提供自定义请求头——
  二者任一成立，Codex 才能连这条网关，到时复测即可。
- Codex CLI 的 npm 包名（实现阶段按 `@openai/codex` 核实过官方文档）与 Claude Code 的安装命令，需要在真实干净机器上端到端跑一次安装验证。
- DeepSeek Harness 的官方安装命令、非交互自证调用方式均未确认：安装/自证对它只给引导、不自动执行；`install_command()` 与 `self_test_command()` 都返回 None。
- Claude Code CLI 二进制名在 Windows 上是 `claude.cmd`（`shutil.which` 按 PATHEXT 找），装了 npm 全局后要新开终端 PATH 才刷新——工具进程内重测前需要用户重开终端或手动刷新 PATH。
- Codex 的 Key 落点是 Windows `setx AI_GATE_API_KEY`（写用户环境变量），这个动作不可备份/不可回滚，UI 已明示"重开终端后生效"；macOS/Linux 下 store_key 只提示手动 export。
- 网关是否校验 `anthropic-version` 协议版本头。目前按不校验处理，对应的检测项默认关闭。
- DeepSeek Harness 仍处于开发者预览阶段，配置结构可能随正式版本调整。程序读取前需要校验配置文件结构是否与预期一致，不一致时提示更新 Suture，而不是按旧结构硬读。
