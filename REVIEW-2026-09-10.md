# 复审发现与修复方案（2026-09-10）

> **状态：P0 + P1 五条已修（`suture` 侧 + 相应测试），P2 待定。**
> 已修：问题 1（明文 Key）、3（非 UTF-8 崩溃）、4（语法错时整文件覆盖）、
> 5（本机请求失败被报成网关侧）、7（托管层无效修复）、6（conflict no-op，
> 与 1 同属一条链路顺手改掉）。未修：2（Windows chmod，属文档口径）、8–12（体验类）。
>
> **2026-09-10 第三轮补充（见文末「第三轮」）：**上面这些修复都只修了**实例**、
> 没修**同类**。第三轮按「类」重新枚举，补齐 5 处漏掉的同类位置，另修掉 2 条
> 「自己写坏自己的文件」的路径。新增 12 条回归用例，共 **121 条全过（跳过 1）**。
>
> **2026-09-10 第四轮补充（见文末「第四轮」）：**第三轮那些「按类修复」本身也写
> 出了回归（就地脱敏抹掉服务端状态、`.env` 层误判、不可写判定没按字段），另发现
> 写入链路不是原子的（序列化在 `open(w)` 之后失败 → 配置文件被清空）。已全部修掉，
> 全量 **137 条全过（跳过 1）**。

对 `main`（`77a4474`）做了一轮只读复审，覆盖判定/可达性/适配器和 界面/服务端 两块，
每条都对着源码核实过，并在临时沙箱里实际复现。基线：
`PYTHONUTF8=1 python -m unittest discover -s tests -t .` → **100 条全过，跳过 1**。

结论：**7 条确认的缺陷**（其中 2 条严重），另有 5 条体验类问题。已确认的产品口径
（能通就算能连、安装页/检查页判装差异、codex 契约不可达）**不在其中**，不再重报。

---

## 一、确认的缺陷

### 1. 明文 Key 回给了浏览器 —— 违反「只出现掩码」的承诺（严重·安全）

三条路径，全部经 `engine.report_to_dict()` → `/api/check` 直达界面：

| # | 位置 | 泄漏内容 |
|---|---|---|
| a | `suture/checks.py:747` `"fix_value": key if auto else None` | 原始 Key（补 Token 头那条修复的载荷） |
| b | `suture/checks.py:224` → `:519-524` `fix_value=value.strip()` | 原始 Key（首尾空格修复） |
| c | `suture/gateway.py:50` `f"请求出错：{exc}"` | 异常原文里带请求头原值 |

`current_value` 掩码了，但同一对象里的 `fix_value` 没有；`e2e.detail` 也没有。

**实测**（token_only 假网关，`ANTHROPIC_API_KEY=yotta_pk_SECRET_ABCD1234`）：

```
state= blocked
RAW KEY present in /api/check payload: True
 issue auth-header | kind= auto | cur= yott****************1234 | fix= yotta_pk_SECRET_ABCD1234
e2e.detail = "请求出错：Invalid header value b'yotta_pk_valid1234\n'"
```

**触发条件**是主线场景：claude 用户把 Key 放在 `ANTHROPIC_API_KEY`、没补
`ANTHROPIC_CUSTOM_HEADERS` 里的 `Token` 头 → e2e 401 → 点「开始检查」即泄漏。

**修法**（改一处即可覆盖 a/b）：`report_to_dict()` 是唯一的出网出口，在那里对每个
issue **删掉 `fix_value` 字段**——前端从不读它（`index.html` 的 `actFix` 只发
`{client_id, issue_id, value}`，`auto` 动作由服务端从 `last_report` 里自己取值），
删掉不影响任何功能。c 另修：`_classify` 的未知分支不要拼异常原文，并对 `e2e.detail`
做一次「已知 Key 值 → 掩码」的替换兜底。

---

### 2. Windows 上 `chmod 600` 是空操作，文档的「权限收紧」不成立（严重·文档与行为不符）

`fixer.py:41-42,53`、`claude_code.py:268,317`、`deepseek.py:294,355,386` 全部依赖
`os.chmod`。Windows 上它只映射只读位，`0o600` 的实际结果是 **0o666**——没有任何
访问控制。含 Key 明文的目标配置和备份文件因此没有被额外保护。

**实测**：`os.chmod(p, 0o600)` 后 `stat.S_IMODE(...) == 0o666`（`os.name == 'nt'`）。

DESIGN.md「备份文件里同样有 Key 明文，权限不收紧等于多留了一份泄露面」与
VERIFICATION 的「备份文件权限 600」在 Windows 上都不成立。

**修法**（二选一，建议后者）：
- Windows 下用 `icacls <path> /inheritance:r /grant:r "%USERNAME%":F` 收紧 ACL；
- 或**如实写进文档**：权限收紧仅在 macOS/Linux 生效，Windows 依赖用户目录默认 ACL。

---

### 3. 非 UTF-8 配置文件（Windows ANSI/GBK）→ 未捕获异常，整轮检查崩溃

三个适配器读文件只接住了各自的解析错误和 `OSError`，没接 `UnicodeDecodeError`
（它**不是** `OSError` 的子类）。用记事本存成 ANSI 的 `settings.json`（含中文注释
或中文值）在面向非技术同事时很常见。

**实测**（claude；codex / deepseek 同型）：
```
CRASH: UnicodeDecodeError 'utf-8' codec can't decode byte 0xd6 in position 29
```

位置：`claude_code.py:107`、`codex.py:38-49`、`deepseek.py:39,87`；写侧
`codex.py:175`、`deepseek.py:279` 同样。
用户侧表现：`/api/check` 是**唯一没有 try/except 的路由**（`server.py:161-165`，
`recheck`/`configure`/`action` 都有），所以请求直接断开，界面只有「检查失败」。

**修法**：三处读取（含两处写前的读）统一加
`except UnicodeDecodeError` → `parse_ok=False` +
「文件不是 UTF-8 编码（可能是 ANSI/GBK），请用编辑器另存为 UTF-8 再试」；
另给 `/api/check` 补 try/except 兜底 500，任何未预期异常都不该让界面断掉。

---

### 4. 文件有语法错时，写入 = 整文件覆盖，静默抹掉其它配置（严重·数据丢失）

`claude_code.py:238` `data = dict(target.data) if target.parse_ok else {}`——
目标层读不懂时用空数据起草再整文件写回，于是「修一个地址」会把同一文件里
permissions / hooks / mcpServers 全部清掉，还报「已修改」。

**实测**（settings.json 多一个逗号 → 点 base_url 的「修复」）：
```
修复前: {"env": {...}, "permissions": {"allow": ["Bash"]}, "hooks": {"Stop": []}}
修复后: {"env": {"ANTHROPIC_BASE_URL": "http://127.0.0.1:5232/zi/proxy"}}
permissions survived? False | hooks survived? False
```
`deepseek.py:314-318` 同型。备份还在（能人工找回），但用户不会知道配置被清了。

**修法**：目标层 `parse_ok=False` 时**拒绝写入**，返回「文件读不懂，请先修好格式
或手动修改」；引擎把这类转成 `none`/`external` 的 issue，不提供「修复」按钮。

---

### 5. 本机请求没发出去，却被报成「网关侧连不上，联系网关值班」（假诊断·高优先级）

Key 末尾带换行（复制粘贴常见）→ `urllib` 抛 `Invalid header value`，没有状态码 →
`e2e_hint()` 返回 `unknown` → `_e2e_fallback` 的兜底（`checks.py:634-640`）给出一条
**high** 的「AI Gate 网关侧暂时连不上…不是本地配置造成的…联系网关值班人员」，
被排序顶到第一条，而真正的原因（Key 带换行、可一键修）排在下面。

**实测**：
```
e2e: {'classification':'unknown','status':None,'detail':"请求出错：Invalid header value b'yotta_pk_valid1234\n'"}
  high   gateway-side | kind= external | AI Gate 网关侧暂时连不上
  medium auth-whitespace | kind= auto   | Key 前后有多余的空格或换行
```

**修法**：`e2e_hint` 增加一类「请求在本地就没构造成功」；`_e2e_fallback` 对它给中性
文案（「请求没能发出去，请先处理下面列出的本地问题」）+ `severity=medium`，且不要
顶到清单最前。同理适用于地址缺协议头导致的 `unknown url type`。

---

### 6. 「多层配置不一致」的自动修复恒为 no-op，这条原因永远清不掉

`checks.py:432-438` 的 `fix_value = rf.value`（**当前生效值**），而 `apply()` 写的
正是生效的那一层——等于把同一个值原地重写，其它层没被统一。

**实测**：项目级 = 正确地址、全局 = 地址 + `/v1` → 点「修复」→ `changed` →
再检查 `conflict:base_url` **原样回来**。同时它与 `base_url` 那条 auto 的写入值
互相矛盾（一条写正确地址、一条写当前值）。

**修法**：该 finding 改成 `FIXABLE_NO` / `external`，文案说明「同一项在多层里
取值不同，请手动统一到生效的那一层」；真要自动统一，得实现「删除其它层的该键」，
不建议这一轮做。

---

### 7. 托管配置（IT 下发）出错时，给出的「修复」写了也不生效，点了原地循环

`checks.py` 的 `env_wrong` 短路（`:804-809`）只判了 `source_layer == "环境变量"`，
漏了托管层。托管配置是只读、优先级最高，而 `claude_code.py:226-233 _target_file`
永远不会选中托管文件 → 写入必然被压过。

**实测**（`SUTURE_MANAGED_SETTINGS_PATH` 指向一份 base_url 多带 `/v1` 的托管配置）：
```
state: blocked
  base_url | kind= auto | fix_value= 'http://127.0.0.1:5225/zi/proxy'   ← 写了不生效
```
`checks.py:422-430` 的 conflict 分支**已经**正确处理了托管层（不给一键修复），
只有 `base_url` / `model` 这两条没有同样的判断。

**修法**：把 `env_wrong` 的短路条件从「来源是环境变量」扩展为「**该字段的生效层
不可写**」（环境变量 或 `managed=True`），统一换成一条外部说明，文案区分两种来源
（环境变量 → 去系统设置改；托管 → 找 IT）。

---

## 二、体验类问题（不阻塞，但会让用户卡住或看不懂）

| # | 位置 | 现象 | 修法 |
|---|---|---|---|
| 8 | `index.html:469-475` + `engine.py:432-434` | `/api/configure` 的 `note`（**唯一的原因说明**）前端从不渲染。codex 用户点「配置」→ 只看到「未保存任何配置」+ 一个还能再点的「配置」按钮 → 死循环 | `msg` 拼上 `r.note`；`r.path === null` 时不重建卡片，只显示说明 |
| 9 | `index.html:591-629` | 安装页按「全局只有一个安装在跑」写：`renderInstall()` 全量重建会重置**其它**卡的 busy 状态，并清掉已写好的输出/横幅 | 模块级「安装中」集合控制禁用态；输出/横幅写到重建后仍存在的固定容器 |
| 10 | `index.html:209-210,631-638` | 令牌过期/服务退出时，界面直接显示英文 `forbidden` / `Failed to fetch`；`reloadState` 的空 `catch` 让安装页一片空白、页头永远「加载中…」 | `api()` 按 status 映射中文（403→「页面已过期，请重新打开 Suture」；网络异常→「本地服务已退出，请重新运行」）；`reloadState` 失败时在页面出提示 |
| 11 | `index.html:566-572,609-617` | 安装命令失败但二进制已装上时，卡片不再渲染输出框，横幅仍说「请查看上方输出」——指着不存在的东西 | 无 outbox 时把输出渲染成独立结果块 |
| 12 | `gateway.py:92,100` + `engine.py:240` | 检查期间无进度、无取消；三个客户端都连不上时最坏约 8 + 3×4×12 ≈ 150 秒只显示「正在检查…」（有界，不是卡死） | 前端按客户端逐个更新状态文案，或服务端逐客户端返回 |

---

## 三、本次明确不动的

- 「能通就算能连、不校验地址是否在网关登记地址内」——已确认口径，含本地转发假绿。
- 「有配置即走真实请求」vs「安装页按可执行文件判装」的差异——预期行为。
- 「未指定模型时用探活模型证明传输/鉴权通即判 connected」——保留。
- codex 在只认 `Token` 头的网关上给「外部处理」——契约驱动，正确。

---

## 四、工具目前能检查出什么（能力清单）

一个客户端一轮判定的结果是四态之一：

```
connected      真实请求通了 → 只给绿勾，不罗列任何细项
blocked        连不上 → 展开「可能的原因」，用户挑着修
unconfigured   装了但一行没配 → 给配置面板（默认模型 + Key）
not_installed  没装 → 引导去「安装 / 上手」页
```

进入 `blocked` 后能给出、且**每条都带具体解决方式**的原因如下。
修复方式分五类：`自动修复`（给按钮）/ `选模型`（候选下拉）/ `填 Key`（输入框）/
`外部处理`（只在工具外做，给说明）/ `只解释`（无动作，给下一步提示）。

### 4.1 配置文件本身

| 能查出 | 解决方式 |
|---|---|
| 文件 JSON/TOML/YAML 语法错，整层不生效 | 只解释：指出文件位置与错误行列，让用户改完重新检查 |
| 字段名拼错（写了工具不认识的键，等同没配这一项） | 只解释：给出最接近的正确字段名 |
| 某层配置存在但实际不会被采纳（Codex 项目不可信） | 外部处理：说明改了没用，指出该改哪一层 |

### 4.2 网关地址

| 能查出 | 解决方式 |
|---|---|
| 没配地址 | 自动修复：写入网关规范地址 |
| 地址前后有多余空格 | 自动修复：去掉空格 |
| 协议写成 http（网关要求 https） | 自动修复：补成 https |
| 少了 `https://` 前缀 | 自动修复：补前缀 |
| 结尾多拼/少拼一段 `/v1` | 自动修复：按该客户端的拼路径习惯改 |
| 地址与网关登记值不一致 | 自动修复：改成登记地址 |
| 同一项在多层配置里取值不同 | 自动修复：统一到生效值（⚠ 见问题 6，当前是 no-op） |
| 本地设置被公司托管配置覆盖 | 只解释：说明改本地没用，不提供按钮 |

### 4.3 鉴权

| 能查出 | 解决方式 |
|---|---|
| 完全没配鉴权信息 | 填 Key |
| 间接引用（`apiKeyEnv`）没写变量名 | 自动修复：补上变量名 |
| 间接引用的变量没设置 / 取不到值 | 填 Key：说明需设置该变量或存入凭据文件 |
| Key 前后带空格或换行 | 自动修复：去掉首尾空白 |
| Key 不是网关签发的（按 `yotta_pk_` 前缀判断，并识别 Anthropic/OpenAI/Google 等厂商前缀） | 填 Key：说明需回后台重新生成 |
| 同一份 Key 在多个位置重复设置、或各位置值不一致 | 只解释：摆出证据（哪个更像网关签发的），让用户确认后删掉多余的 |
| 401 且 Key 有效、但没放进网关要求的 `Token` 头 | 自动修复：补 `headers.Token` / `ANTHROPIC_CUSTOM_HEADERS`（claude / deepseek）；codex 给外部处理 |
| 补头后仍 401，或 Key 前缀不对 | 填 Key：判定为 Key 已失效/被吊销，引导重新生成 |
| 客户端带不了网关要求的自定义头（codex × AI Gate 的 `Token`） | 外部处理：直接说明这条路走不通，建议改用 Claude Code / DeepSeek |

### 4.4 模型

| 能查出 | 解决方式 |
|---|---|
| 大小写 / 连字符写法跟登记型号只差一点 | 自动修复：改成登记写法 |
| 型号不在路由表，但有相近的**独立**型号 | 选模型：给候选下拉，不自动替换（避免连到另一个模型） |
| 型号完全不在路由表 | 选模型：给网关完整型号清单 |
| 没指定模型，且真实请求没通 | 选模型：给完整清单让用户挑一个 |
| 型号在快照里、地址也对，但网关实际 400/404 | 外部处理：提示该型号可能已下线、联系网关 |
| DeepSeek 注册了多个模型，只改其中一个 | 保留其余模型，只替换目标那一项 |

### 4.5 网关侧与整体

| 能查出 | 解决方式 |
|---|---|
| 网关超时 / 连不上 / 限流 / 5xx | **顶层短路**：每个客户端只给一条「网关不可用，本地配置无需更改」，完全不碰本地文件 |
| 客户端里什么都没配，但客户端自己实测能用 | 判「已连接」，不报故障、不给修复按钮（避免盖掉正在工作的连接） |
| 401 不算网关侧问题 | 继续查本地——这正是要抓的场景 |

### 4.6 写配置的安全约定

任何一次写入（修复 / 存 Key / 一键配置）都走 **备份 → 写入 → 用新配置重测**，
备份目录 `~/.suture/backups/<时间戳>/`；写失败或 Key 存失败会尝试回滚并**如实**
报告回滚结果。Key 在界面、日志、命令行、接口返回里只以掩码（前 4 + 后 4）出现
—— ⚠ 当前有例外，见问题 1。

---

## 五、建议的修复顺序

| 优先级 | 项 | 影响面 |
|---|---|---|
| P0 | 1 明文 Key 回界面 | 安全承诺失效，主线路径必现 |
| P0 | 4 语法错时整文件覆盖 | 静默数据丢失 |
| P1 | 3 非 UTF-8 崩溃 | 目标用户（非技术同事）常见，整轮检查不可用 |
| P1 | 5 误报网关侧 | 把用户引向错误方向、且排在最前 |
| P1 | 7 托管层无效修复 | 企业环境下点了没反应 |
| P2 | 6 conflict no-op | 这条原因永远清不掉 |
| P2 | 2 Windows chmod | 文档与行为不符，改文档成本最低 |
| P2 | 8–11 体验类 | 用户卡住/看不懂 |
| P3 | 12 检查无进度 | 有界，可后置 |

---

# 第三轮（打包前）：同类缺陷清扫

> **状态：五个「类」全部修完，新增 12 条回归用例。**
> 基线从 100 条涨到 **121 条全过（跳过 1）**。

前两轮修的是**实例**，不是**类**。这一轮把每个修复点按「还有哪些位置是同一个病」
重新枚举了一遍，结果五个修复里有四个都漏了同类位置——只要换个入口走进去，
同样的 bug 还在。

## 六、只修了实例、没修同类

| # | 上一轮修的是 | 同类位置漏了 | 后果 |
|---|---|---|---|
| ① | `/api/check` 崩溃兜底 | 异常在**构造请求时**就抛了，压根没进 `try` | 地址少写 `https://` → `ValueError` 逃出去 → 界面只剩英文 "Failed to fetch" |
| ② | — | `Popen(["npm", ...])` | Windows 上 npm 只有 `npm.cmd`，`check_runtime` 找得到、执行时必抛 WinError 2 |
| ③ | `settings.yaml` 读不懂不写 | `.credentials.yaml`、codex 的 `OSError` 分支 | 整份凭据被静默覆盖（连带别的厂商的 Key），还报「已保存」 |
| ④ | `report_to_dict` 脱敏 | `/api/action`、`/api/configure` 也返回客户端数据 | 换条路由照样把原始 Key 发给浏览器 |
| ⑤ | `base_url`/`model` 的只读守卫 | `auth` 类字段 | Key 来自系统环境变量时，照样给一个点了没用的「一键修复」 |

### 6.1 本机就构造不出的请求（①）

`gateway.py` 里 `Request(...)` 写在 `try:` 之外。`urllib.request.Request`
在**构造**时就会因为 URL 没 scheme 抛 `ValueError: unknown url type`，鉴权值里
带了换行则抛 `http.client.InvalidURL`——这两类恰恰都有专门的中文说明，却因为
异常绕过 `_classify` 而永远用不上。

**实测**：`ANTHROPIC_BASE_URL=tower-ai.yottastudios.com/zi/proxy`（少 `https://`）
→ 修复前整轮检查抛异常；修复后 `state=blocked`，原因清单一一条 `base_url`。

### 6.2 读不懂的文件不许写 —— 补齐全部写入点（③）

「读不懂就不写」原先只覆盖了会**整文件重写**的两处，而同一条链路上还有：

- `deepseek.store_key` 先写 `.credentials.yaml`：这份文件是**多厂商共用**的
  （`refs` 段），读不懂却照写 = 用只含本次改动的文件覆盖整份凭据，别人的 Key
  被静默抹掉。现在解析失败 / 解析结果不是映射 / `refs` 不是映射，三种情况都
  `RefuseWrite`；能正常读时照旧合并（没为安全砍功能）。
- `codex` 的 `except OSError` 原先降级成「文件是空的」，等于整份 `config.toml`
  被替换成一小段。

`unparseable_write_target` 也补齐了——它原先只认一个层，现在按 harness 把
**所有会整文件重写的层**都报出来，判定层据此不给出点了也写不进去的按钮。

### 6.3 原始 Key 不出网 —— 收成唯一出口（④）

上一轮只在 `report_to_dict`（`/api/check`）里删 `fix_value`。但 `/api/action`、
`/api/configure`、`/api/recheck` **同样**返回一份客户端数据。现在抽成
`engine.redact_client()`，四条路径共用；服务端自己那份 `last_report` 仍保留原值
（点「修复」时要用），只有出网的副本被脱敏。

### 6.4 不给点了也没用的按钮 —— 按类判断（⑤）

`_readonly_source` 那种按**层名字精确匹配**的写法对 auth 类字段无效：
三个适配器给环境变量层的命名各不相同（`环境变量` / `环境变量 AI_GATE_API_KEY` /
`启动时的环境变量` / 两种 `.env`）。新增 `_is_env_like_layer()` 按**来源性质**
判断，`_cannot_write(cfg, keys)` 统一收口，`auth-header` / `auth-ref` / `auth`
三条分支和 e2e 兜底都改走它——要么给能用的按钮，要么老实说「这个得你在工具外改」。

### 6.5 npm 必须先解析成真实路径（②）

Windows 的 `CreateProcess` 不按 `PATHEXT` 补扩展名。`check_runtime` 用
`shutil.which("npm")` 找得到，`Popen(["npm", ...])` 却必抛 WinError 2。现在执行
前先 `shutil.which` 解析成真实路径，两边口径一致；解析不到就给中文指引，
不再把 WinError 抛给用户。

## 七、这一轮顺带发现的「自己伤自己」

这两条不在任何一份审查报告里，是在核对写入链路时发现的：

1. **写出器产出自己读不回来的文件。** `_minimal_yaml` 的写出器会生成 `{}` / `[]`
   （空集合），解析器却对 `[` `{` 一律拒绝 → 下次读取把整份配置判成语法错 →
   触发「读不懂就不写」→ 用户彻底卡住，而且是自己写坏的文件。空集合没有歧义，
   解析器现在放行这两个特例，其余流式写法照旧拒绝。

2. **换行值写进 YAML = 永久锁死。** Key 粘贴时带上换行 → 写出器把换行原样写进
   YAML（YAML 里换行必须用块标量表达，而这个受限写出器不支持块标量）→ 同样得到
   一个自己读不回来的文件。现在写出器遇到换行**明确报错**并提示「多半是复制 Key
   时把换行一起粘了进来」。

   与此配套：`apply_action` 的 auto/choice 分支原先**不 strip**，而它的载荷正是
   「Key 末尾有换行」这个病本身——原样写进去等于把病原封不动搬进新文件。
   `input` 分支一直是 strip 过的，现在两边对齐。

## 八、本轮未修（低危，仅记录）

| 项 | 说明 |
|---|---|
| WebView 起不来时的浏览器兜底是个死胡同 | 回退到浏览器后，该终端窗口仍被 WebView 启动失败占着，用户得自己关；不影响功能，只是体验 |
| 备份目录同一秒内两次备份会同名 | ~~`~/.suture/backups/<时间戳>/` 秒级精度，同秒第二次会落到同一目录（内容不丢，只是混在一起）~~ **这条当时写轻了**：实测是同名文件被**覆盖**，备份里留下的是改过之后的内容，原始那版反而找不回来。第四轮已修（同秒自动改用 `<时间戳>-2`、`-3`…） |
| codex 的「写不进去」判定与执行口径 | 本轮已把 `unparseable_write_target` 补上，与 `apply` 的 `RefuseWrite` 对齐；暂无复现路径 |

## 九、回归用例（`tests/test_packaging_fixes.py`，12 条）

按「类」而不是按「实例」写，每条都带边界输入：

| 用例类 | 覆盖 |
|---|---|
| `TestUnreadableFileIsNeverOverwritten` | `.credentials.yaml` 三种读不懂的形态（非 UTF-8 / 流式写法 / `refs` 是列表）逐字节比对未被改动；能读时仍合并；codex 目标读不出时 `apply` 拒绝 |
| `TestYamlWriterRoundTrip` | 换行值必须报错；空集合必须能往返 |
| `TestRawKeyNeverReachesTheBrowser` | 服务端保留原值、出网副本不含 Key，且**真的走一遍 HTTP 路由**验证 `/api/check` + `/api/action` |
| `TestNoDeadButtons` | Key 来自环境变量时 `auth-header` 是 `external`；配置语法错时**一条**可点按钮都不给 |
| `TestLocalRequestFailureIsHandled` | 少 `https://` 不崩且归因 `base_url`；auto 写鉴权字段会 strip |
| `TestNpmIsResolvedBeforeExec` | 真跑一次 `npm --version`（走 `SUTURE_INSTALL_CMD_*`，不装东西）证明能执行起来；命令行不存在时给中文指引而非 WinError |

---

# 第四轮（2026-09-10）：修第三轮改出来的回归 + 写入链路

> 三路只读审查（本轮改动本身 / 新人流程 / 写入安全与并发），
> 每条都由我独立复现后才认。全量从 121 条涨到 **137 条全过（跳过 1）**。

## 十、第三轮的「按类修复」自己写出的回归

第三轮的教训是「只修实例没修同类」，结果这一轮发现：**那几个修复本身也犯了同类的错**。

| # | 严重度 | 问题 | 复现证据 |
|---|---|---|---|
| R1 | **高** | **修完一项后，同一客户端剩下的「一键修复」按钮全变死按钮。** `_redact_result` 就地 `pop("fix_value")`，而出网的这份 payload 和服务端 `last_report` 里持有的是**同一批 dict**（`_record_client` 用 `ClientState(**{...})` 接过去时 `issues` 是按引用接的）——脱敏把服务端那份也一起抹了 | 服务端那份 fix_value 由 `'SECRET'` 变 `None`；点剩下的 auto 项返回「请选择要应用的值。」而不是修复 |
| R2 | 中 | **deepseek 的 Key 来自 `.env` 时被误判成「改不到」**。`_is_env_like_layer` 里 `name.endswith(".env")` 把 `.env` 归成了环境变量层。但 `store_key` 写的是 `.credentials.yaml`，**优先级高于 `.env`**（解析顺序：启动环境 > 凭据文件 > 项目 .env > harness home 的 .env）→ 写进去一定生效 | 生效层 `项目目录下的 .env` → `_cannot_write=True`（按钮消失）；实测写入后新 Key 确实赢了 `.env`。文案还把人指向「系统设置 → 环境变量」——值根本不在那儿 |
| R3 | 中 | **一份坏的 `.credentials.yaml` 连带封掉 `base_url`/`model` 的按钮。** `unparseable_write_target` 是按客户端整体判的，不分字段 | `.credentials.yaml` 非 UTF-8、`settings.yaml` 完好时：`_cannot_write(base_url)=True`，但直接调 `apply` **成功**。文案说「要修改的配置文件读不懂」——指的却不是真正要改的那个文件 |

## 十一、写入链路：不是原子的

这一类第三轮只修了「读不懂就不写」，没修「写到一半失败」。

| # | 严重度 | 问题 | 复现证据 |
|---|---|---|---|
| W1 | **高** | **deepseek 的 `apply` 先 `open(path,"w")` 截断、后求值 `yaml.dump(...)`。** 写出器拒绝某个值（如带换行的 Key）时抛 `MiniYamlError`，而它既不是 `RefuseWrite` 也不是 `OSError`，**引擎两个 except 都接不住** → 文件已被清空、异常逃逸、无回滚、无中文提示 | `settings.yaml 153 字节 → 0 字节`。claude_code 同款写法：`json.dump(ensure_ascii=False)` 遇孤立代理字符抛 `UnicodeEncodeError` → 写一半的 settings.json |
| W2 | 中 | **一键配置失败会留半成品**：先落盘配置、再存 Key，存失败只塞进 `steps`（前端不渲染）、不回滚；备份还是在生成**之后**做的，备份里存的也是那份占位文件 | api_key 为空 → `{"ANTHROPIC_API_KEY": "请替换为网关后台生成的 Key"}` 落盘，消息只说「配置已保存，但连接未成功」 |
| W3 | 中 | **一键配置把不认识的模型静默换成探活模型**，还报「配置完成，已连接 AI Gate」 | 要 `this-model-does-not-exist`，实际写进 `deepseek-v4-flash`。前端下拉为空（`/api/state` 失败过）时同样命中 |
| W4 | 中 | **`/api/state` 是唯一没有兜底的路由**——`/api/check` 的注释还写着「所有其它路由都有同样的兜底」，而它没有 | 畸形嵌套 JSON → `RecursionError` 逃出 → 连接被直接关掉（`RemoteDisconnected`），前端只能渲染空白页；同一份文件下 `/api/check` 优雅返回 500 |
| W5 | 中 | **安装可以并发**：`/api/install_run` 在锁外且无去重。UI 只 disable 被点的那一个按钮，几秒内点另一个客户端的安装就会真并发（两个 `npm install -g` 写同一前缀） | 实测两个安装重叠执行；加锁后第二个请求返回 409 |
| W6 | 低 | `note` / `steps` **从不渲染**。丢的是「改的是配置文件，正在运行的客户端需重启」「已写入环境变量，重开终端后永久生效」这类关键提醒 | 前端只取 `result/message/client` |
| W7 | 低 | `resolve_command` / `selftest` 用 POSIX `shlex.split` 处理覆盖命令，Windows 路径的反斜杠被当转义符吃掉 | `C:\Program Files\x\py.exe` → `C:Program Filesxpy.exe`，然后报「找不到可执行的」——用户明明写的是存在的路径 |

## 十二、本轮修法

| 项 | 修法 |
|---|---|
| R1 | `redact_client` 改成**返回副本**不再就地改写；`_record_client` 接管时把 `issues` 拷一份自己持有——服务端那份和出网那份彻底分家，以后谁动哪一份都不影响另一份 |
| R2 | `_is_env_like_layer` 去掉 `.env` 分支（`.env` 是文件、且 `store_key` 写的凭据文件优先级更高），并在 docstring 里写清为什么不能算 |
| R3 | `unparseable_write_target(cfg, fields=...)` 增加字段参数：deepseek 只在这次要写 auth 类字段时才把凭据文件算进来 |
| W1 | `base.write_bytes_atomic(path, data)`：先把内容序列化并编码成 bytes，再写同目录临时文件、`os.replace` 原子替换。三个适配器的写入全部改走它；序列化失败转 `RefuseWrite`（引擎已有的「拒绝写入 + 说明原因」路径）；deepseek 的 `dict(...)` 结构强转加了类型守卫 |
| W2 | `configure_client` 改成**先备份、再生成**；存 Key 失败时按备份回滚并如实说明「已放弃这次配置」 |
| W3 | 模型必须是用户真选了、且网关确实有的型号；为空或不在清单里就**什么都不写**、明确告知（跟生成配置时留占位符同一口径：不替用户决定一个他从没选过的模型） |
| W4 | `/api/state` 加与其它路由一致的 try/except，异常返回可读中文 500 |
| W5 | 新增独立 `install_lock`（非阻塞获取）：正在装的时候再点直接返回 409 + 中文说明，不再并发跑第二个安装 |
| W6 | 前端渲染 `r.note`（`/api/action` 与 `/api/configure` 两条路径） |
| W7 | 新增 `installer.split_command`：Windows 上用非 POSIX 模式拆分并手动剥引号，反斜杠不再被吃掉；`selftest` 的覆盖开关共用同一实现 |
| W8 | `backup_files` 同秒自动改用 `<时间戳>-2`、`-3`…，保证每次拿到新目录（这条同时更正了第三轮把影响写轻的记录） |

**回归**：`tests/test_review_round4.py`（16 条），按「用户能看到的行为」写。

## 十三、仍然没修的

| 项 | 说明 |
|---|---|
| 带空格且没加引号的覆盖命令会被拆错 | 跟命令行本身的行为一致（`shlex` 也一样），加引号即可；不改 |
| `Engine._backups` 写了四处从没被读过 | 成功的修复因此没有「撤销」入口，只能去 `~/.suture/backups/` 手工找。要做撤销得先设计交互，这轮不动 |
| 备份目录只增不减、且每份含明文 Key | 同上，属产品决策（自动清理会削弱「能找回」的保证） |
| `/api/recheck` 前端从不调用；`ClientState.generated_path` 没赋值也没读 | 死代码，删它属于清理不属修复，留着不影响行为 |
| `test_server` 偶发失败 | 全量跑 13 次里出现过 1 次（两个用例连接被重置），HEAD 上 6 次未复现，证据不足以定性；两轮全量已稳定通过 |
